interface:
  display_name: "Kimi Ask"
  short_description: "Ask Kimi a read-only repo question"
  default_prompt: "Use $kimi-ask to explain the current repository flow."
policy:
  allow_implicit_invocation: true
