# Roadmap to v1.0.0 GA

Snapshot updated at **v1.0.0-alpha.4 candidate** (2026-05-25, post-user-directive). Captures everything surfaced by three audit rounds + production smoke testing + the user's reset of the thinking-on default. Each item has a triage decision: severity, effort, GA-blocker status, and the proposed approach.

> **This is the historical pre-GA snapshot.** The living post-GA record is the [Post-GA audit log](#post-ga-audit-log) at the end of this file. **For the current shipped version and what it contains, see the "Version" line at the top of [AGENTS.md](./AGENTS.md)** — that is the single source of truth, so this banner no longer restates a version number (it kept drifting a release behind).

Cross-references:
- `CHANGELOG.md` for what's already shipped (alpha.1 → alpha.4)
- `docs/safety.md` for the safety model
- `AGENTS.md` for invariants

## How alpha.3 actually performed

End-to-end smoke testing against kimi-code 0.1.1 confirmed:
- All 9 slash commands work
- PreToolUse hook + setup + check + uninstall lifecycle clean
- `/kimi:cancel` reaps the full kimi → bash chain (this was the alpha.2 regression — fixed)
- Strict-by-default verifier correctly rejected the stale block when the plugin upgraded paths
- 357 tests + drift gate clean

Smoke-test gap that alpha.4 closes: review hung past the timeout budget under default `kimi -p` flags because the budget was sized for the non-thinking path. User directive: keep thinking on everywhere — fix the budget, not the flag.

## What alpha.4 closes

- **G1 (UX blocker for review/challenge)** — raised KIMI_REVIEW_PROMPT_TIMEOUT_MS 600s→1800s, KIMI_ASK 300s→900s, introduced KIMI_RESCUE_PROMPT_TIMEOUT_MS=1800s. Removed `--no-thinking` from `commands/*.md`, `agents/*.md`, `runtime/parsing.ts` SUPPORTED_FLAGS and error templates. The parser now **hard-rejects** `--thinking` and `--no-thinking` with INVALID_ARGS — no escape hatch. (Round 2 multi-agent review caught the parser still advertising the flags after the doc removal; this iteration closes the gap.) Cleaned `kimi-errors.ts` nextStep hints that recommended `--no-thinking`.
- **G3 (functional gap)** — Added `warnIfSessionIdMissing` in `runtime/commands/cli-helpers.ts` and wired it into review/challenge/ask/rescue end-of-job paths. When kimi finishes a job but never announces a session id, the user sees a loud stderr warning that resume/replay won't work for that job. Hypothesis (a) — that `--no-thinking` suppressed the stderr announce — is moot now that thinking is the only documented path. If the warning ever fires in practice we have evidence to investigate hypothesis (b) (capture race) with a real reproducer.
- **L2 (helper duplication)** — Already-fixed-in-alpha.3 (the descendant-tree hotfix incidentally rewrote the cancel path and eliminated the helper duplication round-3 review had flagged).
- **G2 (verifier soft-recovery for nvm/asdf)** — Deferred to v1.1 (see H4 below). The user has not hit this in real workflow; current strict-equality behavior is correct safety-wise and the "Run /kimi:setup" remedy is documented in `docs/safety.md`.
- **G4 (TOCTOU)** — Ships GA with the known-limitation note in `docs/safety.md`. Upstream kimi-code issue still pending.

## GA blockers (must close before tagging 1.0.0)

**Empty as of v1.0.0.** Tagged 2026-05-26. Pre-GA closure sequence:

1. alpha.4 closed the original G1/G3 set (thinking-on budgets + loud session-id warning).
2. alpha.5 work (rolled into the GA tag directly) closed the kimi-code 0.2.0 stream-json session-meta compat gap (G6 — `runtime/stream-json.ts` + `runtime/cli-client.ts` consume the new `role:"meta", type:"session.resume_hint"` record).
3. H6 (wire-protocol version probe, formerly v1.0.x deferred) shipped with GA — `runtime/kimi-version-probe.ts` is wired into `/kimi:setup` to loud-warn on out-of-tested-range kimi versions.
4. Three convergent reviews (Claude opus + Codex via codex-rescue + a kimi 0.2.0 review-smoke under thinking-on) returned consistent verdicts: session-meta is the only 0.1.1→0.2.0 break that affects the plugin; hook contract, exit-code semantics, cancellation behavior, config schema, and output framing are byte-identical between kimi-code 0.1.1 and 0.2.0. Review-smoke additionally caught two issues in the candidate (stderr regex over-widening, missing challenge test), both fixed before the tag.

## High-priority but not GA-blocking (close before 1.1)

### H1 — Hook fail-open on runtime drift (Challenge Finding 1)
**Severity:** Architectural — affects the safety story under environmental change
**Effort:** Large (~2-3 days)

kimi-code's hook protocol treats exit-code-not-0-and-not-2 as ALLOW. If the hook crashes (Node ABI mismatch after upgrade, MODULE_NOT_FOUND, syntax error after a botched rebuild), if `/bin/sh` can't resolve the canonical Node path (LaunchAgent with sanitized PATH), or if it exceeds the 15s timeout, kimi-code silently allows every tool.

Setup's probe validates the install moment; nothing guards runtime drift.

**Decision (for v1.1):** Runtime-side allowlist post-validation. The cli-client receives `tool_calls` records from kimi-code's stream-json; before the model's text response is finalized, re-validate that every emitted tool call is consistent with the current command's read-only allowlist. If a tool call lands that the hook should have blocked, hard-fail the job and surface a loud error. Belt-and-suspenders: hook stays as the primary gate, runtime catches hook escape.

**Status (v1.5.0 — deliberately still deferred).** Considered during the v1.5.0 open-thread sweep and consciously NOT rushed into the batch. Two reasons: (1) it is an explicitly ~2-3 day *architectural* change touching the cli-client hot path that all commands flow through, with subtle semantics (per-label write-class classification, failure semantics, and — for rescue/pursue/swarm-write, which legitimately write — re-running `evaluateRescueHookRequest` on each `tool_call`); and (2) it is **defense-in-depth behind a primary gate that is now field-proven** by the real-binary smokes (read-only write-denial, goal-mode per-turn gating, and the v1.4.1 write-swarm worktree confinement all GREEN against 0.18.0). Rushing a safety mechanism is worse than a clean deferral. Note the detection-after-execution caveat that bounds its value: by the time the cli-client sees a `tool_call` record, a hook that failed open would have already executed the tool — so runtime post-validation is a loud alarm + hard-fail, not prevention (as the original decision states). Remains a good dedicated next effort; the highest-value, lowest-risk slice is read-only commands (any write-class `tool_call` under a read-only label = escape → hard-fail).

### H2 — Session-id stderr format coupling — **CLOSED in v1.0.0**

**Status:** Closed by upstream + GA hotfix. kimi-code 0.2.0 shipped exactly the machine-readable session record this item prescribed: a `role:"meta", type:"session.resume_hint"` stream-json record on stdout (PR #47). The plugin consumes it directly in `runtime/stream-json.ts::validateMeta` + `runtime/cli-client.ts::consumeOutcomes`. The stderr regex remains as a 0.1.x compatibility fallback, anchored to full-UUID payload shape so a malformed line can't pin a garbage id (review-smoke caught the loose form during GA candidate testing).

### H3 — Stream-json parser coupled to kimi-code internals — **CLOSED (v1.0.0 partial → fully delivered v1.2.2)**

**Status (closed pre-GA):**
- Hard-coded line references removed from the doc-comment header of `runtime/stream-json.ts` (the kimi-code 0.2.0 work touched the relevant source lines and proved the line references stale).
- Unknown `meta.type` values are forward-compat: the parser surfaces them via the malformed channel for diagnostics but doesn't crash. This is the same posture H3 prescribed for "unknown record types": skip and continue.

**Remaining for v1.1:** Treating unknown top-level **roles** (not meta types — full unknown `role` strings) as forward-compat. Currently still flagged as malformed because consumers' assumptions about what `records[]` can contain are baked in. Changing this requires a sweep through the command handlers to confirm none of them break on an unrecognized role landing in their iteration.

**DELIVERED 2026-06-09 (v1.2.2).** The sweep (recorded in code comments) found the **only** `records[]` consumer is `reassembleProseFromRecords`, which reads `role === "assistant"` exclusively and skips everything else (`onRecord` has no production caller). So unknown roles are safe either way. Rather than churn the role-keyed `StreamJsonRecord` union (and its `.role` narrowing) by admitting an arbitrary-role member, an unknown non-empty **string** role is now surfaced **out-of-band** on `StreamJsonOutcome.unknownRecord` (`{role, raw}`) — the same posture as `meta`/`goalSummary` — and `cli-client` logs it (`event:"unknown_record"`) but keeps it out of `records[]`/`onRecord`. It is no longer counted as `malformed` (forward-compat framing, not error). A role-less line that isn't `goal.summary`, or a non-string role, stays `malformed` (genuinely unexpected). Tests: `stream-json.test.ts` (unknownRecord vs. malformed matrix), `cli-client.test.ts` (out-of-band, not malformed, assistant still captured).

### H5 — Per-spawn thinking control via kimi-code CLI (was alpha.4 internal bug)
**Severity:** Functional — review-gate's 8s budget assumes thinking-off but cannot enforce it
**Effort:** Negotiation with kimi-code team + tiny buildArgs wiring on our side

kimi-code 0.1.1 has no `--no-thinking` / `--thinking` CLI flag. Thinking is controlled by `default_thinking` in `~/.kimi-code/config.toml` plus optional `[thinking].mode` override. The Round 2 Codex review caught alpha.4's broken attempt to emit `--no-thinking` in argv (kimi-code uses `allowUnknownOption(false)` and would crash the spawn).

The `CliClientOptions.thinking` field stays in the runtime contract as a reserved seam. When upstream lands a CLI flag, `buildArgs` translates it.

**Decision (for v1.1):** File an upstream kimi-code issue requesting `--thinking-mode <auto|on|off>` (or equivalent). Until upstream lands, review-gate behaves correctly only when the user has `default_thinking = false` or selects a non-thinking-capable model — documented in `docs/safety.md`.

### H4 — Verifier soft-recovery for Node version manager switches (was G2) — **DELIVERED in v1.2.1 (as diagnosis, not auto-refresh)**
**Severity:** UX-pain for users on nvm/asdf/mise/fnm
**Effort:** Medium (~half day)

**Delivered 2026-06-09 (v1.2.1).** Shipped as **precise drift diagnosis**, deliberately NOT the auto-refresh in the original decision below. `runtime/hooks/install-paths.ts` gained `parseHookShellCommand` (inverse of `buildHookShellCommand`) + `describeHookCommandDrift`; `evaluateInstalled` takes an optional `nodeExists` predicate and, on a structurally-valid command mismatch, classifies it — "Node binary drift" (pinned interpreter gone → the classic version-manager/Homebrew move), "Node binary changed" (both exist), or "Hook script path drift" (plugin moved/updated) — with the actionable `/kimi:setup` remedy. Surfaced from both `verifyHookInstalled` (the runtime gate behind rescue/swarm/pursue refusal + read-only warnings) and `/kimi:setup --check`. **Why diagnosis over auto-refresh:** silently rewriting the security-config block from a read-only verify path was judged riskier (surprising mutation of a security surface, could mask a real problem) than a clear, classified message; the strict exact-equality decision and the manual `/kimi:setup` remedy stay. Hash-based verification (long-term, below) remains unbuilt — not needed once the diagnosis makes the strict failure legible. Tests: `tests/runtime/install-paths.test.ts` (parser round-trip + classifier matrix), `tests/runtime/hook-install.test.ts` (node-drift + path-drift integration).

Promoted from GA-blocker to v1.1 candidate in alpha.4 triage. Current strict-equality verifier rejects a managed block whose Node binary path no longer matches `process.execPath` — correct safety posture but forces a re-run of `/kimi:setup` after every Node version switch. Documented in `docs/safety.md` under "Known limitation."

**Decision (for v1.1):** Two-pronged.
1. **Short-term:** Add a soft-recovery path — when the installed block's Node binary path differs from canonical but everything else matches AND the installed binary still exists, emit a stderr warning and auto-refresh the block (call setup install logic from verify path). Preserves strict-by-default but adds an explicit "we noticed your Node moved, refreshing the hook" UX.
2. **Long-term:** Hash-based verification — store SHA256 of `dist/hooks/approval-hook.js` in the managed block, verify the hash matches regardless of Node path.

### H6 — Wire-protocol version guard for future kimi-code minors — **CLOSED in v1.0.0**

**Status:** Shipped with GA via `runtime/kimi-version-probe.ts` + `/kimi:setup` integration. The setup probe spawns `kimi --version`, parses output, and pushes a loud warning to setup's warnings array when the version is outside the tested range. Soft-fail policy: probe failures (kimi missing, spawn error, unparseable output) record nothing. Since v1.8.0, safety-sensitive complete-config validation still probes the installed version to apply the correct upstream hook-event schema and fails closed when that version cannot be established; `KIMI_PLUGIN_CC_SKIP_VERSION_PROBE=1` suppresses only the compatibility-range warning probe, not this schema-selection gate.

When kimi-code 0.3.x lands, the only release-engineering step is to extend `KIMI_TESTED_MINORS` after end-to-end verification.

> **2026-05-27 follow-up (initial)**: 0.3.0 and 0.4.0 were audited (reports 31-35). `KIMI_TESTED_MINORS` deliberately held at `[{0,1}, {0,2}]` pending H7 (real-binary CI smoke) as the actual claim-enforcer.
>
> **2026-05-27 follow-up (v1.0.1 release)**: extended `KIMI_TESTED_MINORS` to `[{0,1}, {0,2}, {0,3}, {0,4}]` and shipped as v1.0.1 (tag `v1.0.1`). The trigger was a user-visible UX gap: `/kimi:setup` was warning users on 0.3.0/0.4.0 — versions we had explicitly audited — which read as a contradiction. Decision was to bump the constant now (manual audit is the claim-enforcer for this release) and let H7 retroactively strengthen the assertion. The "user-visible signal beats H7-first sequencing" call lives in the corresponding memo at `projects/kimi-plugin-cc/memos/2026-05-27-first-post-ga-upstream-compat-audit-playbook.md` (memex vault).

### H7 — Real-binary smoke against the installed kimi-code (local smoke DELIVERED 2026-05-28; CI scaffolding DELIVERED 2026-06-04)
**Severity:** Architectural — closes the structural-fragility concern from the 2026-05-27 audit's adversarial review (report 34 §1) and the 0.5.0 `rpc`-undefined tripwire (report 39 F1)
**Effort:** Medium (~1 day) — local smoke done; CI job remaining

Today `bun test` stubs the kimi process — no test exercises the full policy queue end-to-end against a real `kimi -p` binary. PR #26's policy queue refactor made our hook position (index 0) source-code-position-dependent with no upstream invariant test. A future upstream PR could reorder the queue and silently bypass our safety hook with no test failure on either side.

**Delivered (2026-05-28):** `tests/runtime/real-binary-smoke.test.ts` spawns the real `kimi -p --output-format stream-json` in an isolated `KIMI_CODE_HOME` (seeded from the operator's config + OAuth/credentials so it never mutates the real config or session store), installs the managed block via the real `runSetup` path, and for each of review / challenge / ask / review_gate forces a write attempt. It asserts (a) the target file is NOT created in the workspace and (b) the hook deny marker appears in the run — proving the model *attempted* a write and the hook *blocked* it, not that the model declined. Opt-in: skipped unless `KIMI_PLUGIN_CC_SMOKE=1` plus a resolvable binary + authenticated seed home, so `bun run check` stays green in CI/fresh clones. Run via `bun run smoke:real`. Verified green across all four labels against kimi-code 0.5.0 (≈10–12s per label).

**CI scaffolding delivered (2026-06-04).** Two GitHub Actions workflows (the repo previously had **no CI at all** — it relied on local `bun run check`):
- `.github/workflows/ci.yml` — `bun run check` on every push to `main` + every PR. No secrets, no kimi binary, smokes auto-skip. The per-push build/typecheck/test/drift net.
- `.github/workflows/smoke.yml` — the real-binary smokes (read-only **and** the `/kimi:pursue` goal-mode gate) on **manual dispatch**. Installs a pinned kimi-code via the temp-binary technique (`KIMI_PLUGIN_CC_KIMI_BIN`).

**The old "OAuth-only blocker" premise was wrong.** This item assumed kimi-code authenticates *only* via the `managed:kimi-code` OAuth store, requiring a brittle base64 credentials bundle as a secret. But kimi-code's **env-model channel** (`KIMI_MODEL_*`, `config/env-model.ts`, present since 0.6.0 / PR #212) is a clean single-API-key auth: set `KIMI_MODEL_NAME` + `KIMI_MODEL_API_KEY` and it synthesizes the default provider — no OAuth, no expiry. The smoke gate now accepts it (`tests/runtime/real-binary-smoke.test.ts`), and `smoke.yml` uses it. See `docs/ci.md`.

**Remaining (deliberate choices, not blockers):** (1) the operator adds `KIMI_MODEL_API_KEY` + `KIMI_MODEL_NAME` as repo secrets to activate `smoke.yml` (inert until then); (2) the smoke is **dispatch-only, not per-push/scheduled, by design** — a goal-mode run is ~1–2 min of real model tokens (the model retries the deny-wall for the full `--budget`), so per-push would bill every push. Flip on a `schedule:` trigger if continuous verification is wanted. Because the gate isn't yet *continuously* auto-run, `/kimi:pursue` stays labeled **experimental** (also honest because kimi-code's `goal-command` is itself an off-by-default experimental flag upstream).

### H8 — `/kimi:setup` surfaces installed kimi-code plugins (new for v1.1) — **DELIVERED in v1.2.1**
**Severity:** UX — closes the turn-waste concern from the 2026-05-27 audit's adversarial review (report 34 §4)
**Effort:** Small (~half day)

**Delivered 2026-06-09 (v1.2.1).** `runtime/commands/setup.ts::collectInstalledKimiPluginsNotice` reads `~/.kimi-code/plugins/installed.json` (path + `{plugins:[{id,enabled,…}]}` shape re-verified against `packages/agent-core/src/plugin/store.ts` at 0.12.0) and, on both the install and `--check` paths, pushes a non-blocking notice naming installed+enabled plugins with a one-liner that their tool calls are denied (and waste turns) under the read-only commands. Best-effort: missing/unreadable/malformed manifest → no notice; never blocks setup; never mutates the plugin list. Test: `tests/runtime/setup.test.ts` (H8 notice + absent-manifest cases).

kimi-code 0.4.0 (PR #119) added user-global plugin installation. Plugin-provided MCP tools register on every session creation. Our PreToolUse hook denies any unrecognized tool name for review/challenge/ask/review_gate, so plugins don't escalate privilege — but they DO add tool calls that get denied silently inside the kimi turn, wasting model turns. Users would file confused "kimi did nothing useful" bugs.

**Decision (for v1.1):** During `/kimi:setup`, probe `~/.kimi-code/plugins/installed.json` (kimi-code's home dir is `~/.kimi-code/`, not `~/.kimi/`; confirmed against `packages/agent-core/src/plugin/store.ts` + `docs/en/configuration/data-locations.md` in 0.4.0) and emit a non-blocking notice listing any installed kimi-code plugins, with a one-liner explaining that their MCP tool calls will be denied under review/challenge/ask. Don't block setup. Don't mutate the plugin list. Just set the user's expectations.

### H9 — Pin known-good kimi-code version range at setup (new for v1.1, extends H6) — **DELIVERED in v1.2.1 (clarification)**
**Severity:** Architectural — completes the H6/H7 pair
**Effort:** Small (~quarter day, once H7 lands)

**Delivered 2026-06-09 (v1.2.1).** Set-membership in `isInTestedRange` already warned for ANY untested `{major,minor}` (including above the range), so no separate `UPPER_BOUND` gate was needed. Added `maxTestedMinor()` and taught `formatVersionOutOfRangeWarning` to distinguish the common, less-suspicious case — a kimi-code release NEWER than our newest tested minor ("likely fine, but unverified; behaviors may have changed since our last compat audit") — from a below/gap version. This is the user-facing signal the adversarial review asked for (we've verified the index-0 hook contract up to this max). Test: `tests/runtime/kimi-version-probe.test.ts` (above-max note + `maxTestedMinor`).

H6 ships a "warn if version outside tested range" probe. H7 makes "tested" a real assertion (CI-enforced). H9 closes the loop by pinning a known-good upper bound (`>=0.2.0 <0.5.0` once 0.4.0 ships through H7) and emitting a setup-time warning above the upper bound. Adversarial review (report 34 §1) specifically called out that the hook-policy-index-0 contract is undocumented upstream — pinning a tested upper bound is the user-facing signal that we have verified the contract holds for this range.

**Decision (for v1.1):** Once H7 lands, extend `KIMI_TESTED_MINORS` with each newly-verified minor and add an `UPPER_BOUND` constant. Setup-time warning when `kimi --version` reports anything above the upper bound.

## Low-priority polish (open backlog)

### L1 — `dist/` drift hazard for developers who skip `bun run check` (Challenge Finding 7)
The drift gate catches forgotten rebuilds when developers run `bun run check`. Developers who only run `bun test` would miss it.

**Decision:** Document in `AGENTS.md` (already done). Add a `.husky` pre-commit hook in a follow-up if the gate ever silently fails.

### L3 — alpha.3 hotfix invariant note in `runtime/cli-client.ts`
Comment block at the start of the cancellation section should explain WHY we have both per-pid kill AND per-pid negative-pid kill AND the (now removed... wait, we kept it for defense-in-depth) root group-kill. A future maintainer might think the per-pid PGID kill is redundant with the root group-kill — explain the kimi-code spawn-shape reasoning.

**Decision:** Documentation polish; can ship any time.

## Anti-roadmap (deliberately out of scope)

- **Windows process-group reaping.** kimi-code 0.1.1 effectively doesn't support Windows with the same robustness; targeting POSIX-first is correct.
- **Reverting to in-band approval policy (v0.4 wire-style).** The cutover decision is firm — kimi-code dropped Wire transport. Defense-in-depth via H1 covers the same risk surface.
- **Reintroducing `--thinking` / `--no-thinking` as documented user flags.** User directive in alpha.4: thinking is always on for user-facing commands. The parser **hard-rejects** both flags with `INVALID_ARGS` (`THINKING_FLAG_REMOVED_MESSAGE`); there is no escape hatch. review-gate's runtime caller pins thinking-off via `CliClientOptions.thinking` as a reserved internal seam (currently a no-op since kimi-code 0.1.1 has no per-spawn CLI control — see Codex Round 2 finding).

## Ship-or-fix gate for GA — **MET**

**v1.0.0 tagged 2026-05-26.** Pre-tag state:
- alpha.4 closure scope (G1 + G3 + L2) ✓
- kimi-code 0.2.0 stream-json session-meta hotfix ✓
- H6 wire-protocol version probe ✓
- H2 closed by upstream + plugin consumption ✓
- H3 partially closed (unknown meta types forward-compat; unknown top-level roles deferred to v1.1) ✓
- Codex post-hotfix audit clean ✓
- Claude opus audit clean ✓
- kimi 0.2.0 review-smoke clean (8m 37s under 1800s thinking-on budget) ✓
- 391 tests pass, drift gate clean ✓

Remaining v1.1 items: H1 (hook fail-open runtime drift), H3 partial (unknown top-level roles), H4 (Node version manager soft-recovery), H5 (per-spawn thinking control via kimi-code CLI, upstream-blocked), H7 (real-binary CI smoke), H8 (surface installed kimi-code plugins at setup), H9 (pin known-good kimi-code version range — extends H6).

## Post-GA audit log

- **2026-05-27** — kimi-code 0.3.0 (released 2026-05-26) and 0.4.0 (released 2026-05-27) audited by 4 independent reviewers (hook contract, stream-json, CLI surface, adversarial). Verdict: COMPAT-PRESERVED. No runtime changes required. Findings:
  - `packages/agent-core/src/agent/hooks/` byte-identical 0.2.0→0.4.0
  - `apps/kimi-code/src/cli/run-prompt.ts` byte-identical 0.2.0→0.4.0
  - PR #26's policy queue refactor places `PreToolCallHookPermissionPolicy` at index 0, before `auto-mode-approve` (4) and `yolo-mode-approve` (13) — our hook still fires first
  - All argv flags we use (`-p`, `-r`, `--output-format stream-json`, `-m`, `--skills-dir`) unchanged
  - `kimi -p` still hard-codes `permission: 'auto'`; resumed sessions still force-overridden to `'auto'`
  - Adversarial finding: hook-policy-index-0 is convention-only with no upstream invariant test → tracked as H7 (real-binary CI smoke) and H9 (pin upper bound)
  - New plugin system (PR #119) introduces silent turn-waste for users with kimi-code plugins installed → tracked as H8
  - Edits applied: AGENTS.md compat-range framing, `runtime/stream-json.ts` source-of-truth comment widened to "verified through 0.4.0", H7-H9 backlog added here.
  - Reports: `.claude/kimi-code-research/reports/31-upstream-04-hook-contract.md`, `32-upstream-04-stream-json.md`, `33-upstream-04-cli-surface.md`, `34-upstream-04-adversarial.md`, `35-upstream-04-synthesis.md`.

- **2026-05-28** — kimi-code 0.5.0 (released same day) audited by the same 4-reviewer playbook. Verdict: COMPAT-PRESERVED (unanimous). Patch release **v1.0.2** extends `KIMI_TESTED_MINORS` to include `{0, 5}` and tags `compat-verified-kimi-code-0.5.0`. Findings:
  - Hook engine relocated `packages/agent-core/src/agent/hooks/` → `packages/agent-core/src/session/hooks/` (commit `74e867a`) with byte-identical file contents; PreToolUse contract preserved end-to-end
  - `apps/kimi-code/src/cli/run-prompt.ts` and `packages/agent-core/src/rpc/events.ts` byte-identical 0.4.0→0.5.0 — stream-json wire format unchanged
  - `PreToolCallHookPermissionPolicy` still at policy index 0; queue order byte-identical to 0.4.0
  - Argv unchanged; new `--auto` flag is rejected when combined with `-p` (`options.ts:43–45` throws `OptionConflictError`) so it cannot bypass hook-based read-only enforcement
  - `--continue` resume + `forcePromptPermission` still override resumed-session permission to `'auto'`
  - The 1580-line `04-wire-records.diff` is internal persisted-AgentRecord changes (blobref offloading, migration v1.3) that do not surface through `kimi -p --output-format stream-json`
  - Adversarial finding (latent, does not bite in 0.5.0): `AgentOptions.rpc` downgraded from required to `SDKAgentRPC | undefined`; `PermissionManager.requestToolApproval` now silently auto-approves `'ask'` outcomes when `agent.rpc === undefined`. Every Agent constructor site in 0.5.0 production passes a non-undefined `rpc` proxy, so the branch is dead code, but type safety has weakened. Tracked as a v1.1 tripwire: re-verify rpc-supplied-at-every-callsite in future audits and check whether `installHeadlessHandlers` is still wired into prompt mode
  - Edits applied: `runtime/kimi-version-probe.ts` (`KIMI_TESTED_MINORS` extended), `runtime/stream-json.ts` (verified-through range extended to 0.5.0, resume-hint emission position clarified as session end), `runtime/hooks/approval-hook.ts` (path breadcrumb updated to `session/hooks/runner.ts`), AGENTS.md (Upstream compat paragraph, dual-source paragraph clarified), 5-file version bump 1.0.1 → 1.0.2
  - Reports: `.claude/kimi-code-research/reports/36-upstream-05-hook-contract.md`, `37-upstream-05-stream-json.md`, `38-upstream-05-cli-surface.md`, `39-upstream-05-adversarial.md`, `40-upstream-05-synthesis.md`.

- **2026-05-28 (follow-up to the 0.5.0 audit)** — H7 local real-binary smoke **delivered** in response to the report 39 F1 `rpc`-undefined tripwire. The finding is structurally unreachable through our subprocess path (our hook returns only `allow`/`deny` at policy index 0, never an `'ask'` outcome; `kimi -p`'s own auto-approve resolves the rest before any `'ask'` fallback — so the auto-approve-on-undefined-`rpc` branch is dead code for us twice over). No code fix is possible or needed on our side; the gap was that the index-0/deny chain was convention-verified, not test-verified. `tests/runtime/real-binary-smoke.test.ts` now asserts it end-to-end against the real binary (review/challenge/ask/review_gate each force a write → hook denies → no file written + deny marker present). Verified green across all four labels against kimi-code 0.5.0. Opt-in (`bun run smoke:real`); skipped under `bun run check`. CI wiring remains pending on an OAuth-credentials secret (see H7). No runtime changes; test + `package.json` `smoke:real` script + docs only.

- **2026-05-31** — kimi-code 0.6.0 (npm `latest`) audited by the same 4-reviewer playbook. Verdict: COMPAT-PRESERVED (unanimous), and for the first time **backed by a green real-binary smoke against the audited release** (`bun run smoke:real` vs the installed 0.6.0: 5 pass / 0 fail, all four read-only labels deny the forced write end-to-end). Patch release **v1.0.4** extends `KIMI_TESTED_MINORS` to include `{0, 6}` and tags `compat-verified-kimi-code-0.6.0`. Findings:
  - Hook engine (`session/hooks/`), policy queue order (`policies/index.ts`, hook still index 0 / `AutoModeApprovePermissionPolicy` at index 4 — the 5th policy), `pre-tool-call-hook.ts`, and `auto-mode-approve.ts` all byte-identical 0.5.0→0.6.0
  - CLI argv (`options.ts`/`commands.ts`) byte-identical (blob-SHA verified); `--auto` still rejected with `-p`; stream-json resume-hint writer (`writeResumeHint`/`PromptJsonWriter`) byte-identical by content — `session.resume_hint` unchanged
  - `run-prompt.ts` gained a +17-line resume-session workDir guard (`resolvePromptSession`, resume branch only); it runs *before* `forcePromptPermission` as a fail-closed gate and **structurally cannot fire for the plugin** — both resume sites (`ask.ts`, `rescue.ts`) spawn with `cwd: job.cwd`, the same SQLite value the session token was created under, so `target.workDir !== workDir` is always false. Invariant to preserve: never resume a captured token from a different cwd
  - The 0.5.0 latent tripwire evolved: `permission/index.ts` inverted the auto-approve fallback to `if (rpc?.requestApproval) {requestApproval} else {approved}` (and `agent/index.ts` widened `rpc` to `Partial<SDKAgentRPC>`). This is a genuinely new silent-approve branch when `rpc` exists but `requestApproval` is absent — but it is **dead code in `-p` mode** (only the `'ask'` path reaches it, shadowed by `AutoModeApprovePermissionPolicy` at index 4; and `requestApproval` is a required `SDKAgentAPI` member, always present in the CLI host). A hook `{kind:'deny'}` routes through `case 'deny' → {block:true}` with no rpc involvement. Confirmed structurally inert by the green smoke
  - Large internal `runtime.kaos → agent.kaos`/`session.kaos` plumbing refactor (CHANGELOG "Split RuntimeConfig into Kaos and ToolServices") preserves `agent.config.cwd === agent.kaos.getcwd() === workDir` — rescue cwd-confinement and pathClass unchanged. `provider-manager.ts` `ModelProvider`/`SingleModelProvider` extraction is internal (auth/model selection only)
  - `records/` removed `context.mark_last_user_prompt_blocked` — UserPromptSubmit-hook plumbing only (the plugin installs only a PreToolUse hook); never serialized to stream-json; cross-version resume is safe (`restoreAgentRecord` has no throwing `default`)
  - Two doc-only findings (NOT write bypasses): PR #186 removed the default 1000-step/turn cap (compute guardrail; bounded by the plugin's AbortController) and PR #212 added an opt-in `KIMI_MODEL_*` inherited-env channel (never touches hook config, stripped on config write-back). Both noted in `docs/safety.md` § "What this safety story does NOT cover". Experimental-flags infra (`flags/registry.ts`) ships with `FLAG_DEFINITIONS = []` — no flag can disable the hook
  - Smoke caveat (recorded per playbook): the first smoke run died at `auth.login_required` because the operator's local OAuth token had expired (machine state, not a 0.6.0 regression); after re-login it ran green. A skipped/auth-failed smoke is not a passed smoke — the green run is the one that counts
  - Edits applied: `runtime/kimi-version-probe.ts` (`KIMI_TESTED_MINORS` extended to `{0,6}`), `runtime/stream-json.ts` (verified-through range widened to 0.6.0, qualified that the run-prompt.ts change is the resume guard outside the writer), AGENTS.md (Version, Upstream-compat paragraph, dual-source paragraph), `docs/safety.md` (two findings), 5-file version bump 1.0.3 → 1.0.4
  - Reports: `.claude/kimi-code-research/reports/47-upstream-06-hook-contract.md`, `48-upstream-06-stream-json.md`, `49-upstream-06-cli-surface.md`, `50-upstream-06-adversarial.md`, `51-upstream-06-synthesis.md`.

- **2026-06-01 (forward-scan, not a triggered audit)** — Routine upstream check found **no new release**: npm `latest` is still `0.6.0` (published 2026-05-29, no `next`/`beta` dist-tags), GitHub `Latest` is `0.6.0`, local `kimi --version` is `0.6.0` — all already covered by v1.0.4 / `compat-verified-kimi-code-0.6.0`. No 4-reviewer audit, version bump, or tag warranted. As a free look at the *next* release, diffed `origin/main` (`a580cd3`, 12 commits ahead of the `@moonshot-ai/kimi-code@0.6.0` tag) against the six consumed surfaces. Provisional verdict on unreleased main: **COMPAT-PRESERVED**. Findings:
  - **Hook engine** (`session/hooks/`) and **permission policy queue** (`permission/`): **0-byte diffs** — byte-identical. The two surfaces the entire safety model rests on are untouched.
  - **CLI argv** (`options.ts`, `commands.ts`): byte-identical — no diff. `run-prompt.ts` gained only +2 lines adding `cron.fired` and `warning` to the *ignored*-events switch in `runPromptTurn`; both fall through to `return` (filtered, never written as stream-json records). Forward-compat-benign.
  - **Wire/records** (5.5 KB diff): internal-only. New `micro_compaction.apply` record type (#219, MicroCompaction) with restore-time stamping (`_restoring` widened from `boolean` to `RestoringContext | null`); `adaptiveThinking` provider plumbing (#232, `KIMI_MODEL_ADAPTIVE_THINKING`); `cancelAll(reason)` now propagates an abort reason to in-flight subagent tools (#236). `AGENT_WIRE_PROTOCOL_VERSION` stays `1.3` (gained a clarifying comment that new feature record types do not require a bump). None of this changes the stream-json **output shape** our parser consumes, and our cancellation is OS-signal / process-group based — independent of kimi's internal abort-reason plumbing.
  - **Re-confirm at next release (likely 0.7.0):** (1) `warning`/`cron.fired` stay filtered out of `-p` stdout (not surfaced as parseable records); (2) the stream-json parser shrugs off `micro_compaction.apply` if it ever reaches stdout. Both expected to pass; both are exactly what `bun run smoke:real` + the stream-json reviewer will verify when the release actually lands.
  - No edits to runtime, docs, or version files; no commit or tag (per the "don't tag for zero-code-change audits" anti-pattern — and the scanned code is still unreleased and will change before shipping). This bullet is the only artifact.

- **2026-06-03** — kimi-code shipped **three minors in five days** (0.7.0 + 0.8.0 on 2026-06-02, 0.9.0 on 2026-06-03), a 61-commit catch-up from the verified 0.6.0. Certified the whole jump in one cumulative 0.6.0→0.9.0 pass via the playbook's 4-reviewer audit **plus an independent cross-model (codex) adversarial pass**. Verdict: **COMPAT-PRESERVED** (unanimous across all 5 review streams), backed by a GREEN `bun run smoke:real` against **both** the installed 0.8.0 binary and a temp-installed 0.9.0 binary (via the `KIMI_PLUGIN_CC_KIMI_BIN` override — auth seeded from the real home; never mutated the operator's install). Patch release **v1.0.5** extends `KIMI_TESTED_MINORS` to `{0,7}`, `{0,8}`, `{0,9}` and tags `compat-verified-kimi-code-0.9.0`. Findings:
  - The 2026-06-01 forward-scan predictions all held: `warning`/`cron.fired` stay filtered out of `-p` stdout; the parser shrugs off internal feature records (`micro_compaction.apply` etc.) — none reach the `-p` stream-json surface. Hook engine (`session/hooks/engine.ts`, `runner.ts`) and the stream-json writer (`writeResumeHint`/`PromptJsonWriter`) are byte-identical 0.6.0→0.9.0; `policies/index.ts` still puts `PreToolCallHookPermissionPolicy` at index 0 (auto-approve index 4).
  - **Permission approval hooks (#336, 0.8.0)** — `PermissionRequest`/`PermissionResult` added to `HOOK_EVENT_TYPES`. Fire-and-forget OBSERVABILITY only (`fireAndForgetTrigger` + leading `void`, never call `blockDecision`), emitted only inside the `rpc.requestApproval`/ask branch — dead in `-p` auto mode (shadowed by auto-mode-approve), structurally incapable of denying. No enforcement migration; keep PreToolUse-deny as-is (report 57).
  - **Headless goal mode (#270, 0.8.0)** — `kimi -p "/goal ..."` → `runHeadlessGoal`, double-gated behind the experimental flag `goal-command` (default false, env `KIMI_CODE_EXPERIMENTAL_GOAL_COMMAND`) AND a `/goal`-prefixed prompt. Unreachable for the plugin (sets no `KIMI_CODE_EXPERIMENTAL_*`, never sends `/goal`). Confirmed by report 58 that even *inside* the continuation loop the PreToolUse hook fires on every tool call (index 0) — no autonomous-continuation approval bypass. Goal mode also writes a `{"type":"goal.summary",...}` stdout line (no `role`) before the resume hint; it routes to the parser's malformed channel (fail-safe), and is only emitted in goal mode anyway.
  - **`deny-all` policy (#338, 0.9.0)** — `unshift`-ed only onto SUBAGENT policy stacks (`subagent-host.ts:199`) for the `/btw` side-channel; a deny (more restrictive), never the main `-p` agent. The sole `policies` mutation in the 0.9.0 tree.
  - **Default-approve set grew** — `GetGoal`/`SetGoalBudget`/`UpdateGoal` added (`CreateGoal` is NOT). No fs/git/config side effects; the plugin's read-only enforcement is allow-list/deny-by-default, so new upstream tools can't slip through.
  - **New sibling subcommands** (`provider`/`acp`/`login`/`upgrade`) and `KimiHarness`→`createKimiHarness` factory: do not alter `-p` parsing or permission/hook defaults. ACP (#368) is an IDE transport the plugin never spawns.
  - **Doc-only finding (NOT a write bypass):** background auto-upgrade (#334, 0.8.0) is on by default; the plugin's own `-p` spawns never swap the binary (source forced `unsupported`), but a user's interactive TUI can drift to an unaudited version out-of-band. The version probe is the net; raises priority of **H9**. Added to `docs/safety.md` § "What this safety story does NOT cover" (plus a note that the new permission hook events are observability, not a decision surface).
  - Edits applied: `runtime/kimi-version-probe.ts` (`KIMI_TESTED_MINORS` → `{0,7}/{0,8}/{0,9}`), `runtime/stream-json.ts` (verified-through widened to 0.9.0), AGENTS.md (Version, Upstream-compat paragraph, dual-source paragraph), `docs/safety.md` (auto-upgrade drift + permission-hook-events items), `docs/upstream-compat-audit.md` (self-upgrade note), this log entry, 5-file version bump 1.0.4 → 1.0.5.
  - Reports: `.claude/kimi-code-research/reports/52-upstream-09-hook-contract.md`, `53-…-stream-json.md`, `54-…-cli-surface.md`, `55-…-adversarial.md`, `56-…-feature-parity-map.md`, `57-…-permission-hooks-deep-dive.md`, `58-…-goal-mode-feasibility.md`, `59-…-codex-adversarial.md`, `60-…-synthesis.md`.
  - **Feature track (new this audit, for v1.1 decision — NOT shipped in v1.0.5):** the 0.6→0.9 wave is largely Claude-Code-parity, but almost all of it is TUI-only or internal model-quality plumbing invisible to a `kimi -p` wrapper. Exactly one feature creates new plugin surface — **headless goal mode → a flagged `/kimi:pursue` command** (report 58: GO-for-prototype; safe because the PreToolUse hook gates every continuation turn; the real risk is unboundedness, bounded by a mandatory plugin-side AbortController wall-clock ceiling), paired with an S-effort **`goal.summary` parser hardening** (report 56 §2). MONITOR: auto-upgrade drift (→ H9), repeated-tool-call loop detection (#364, free quality win), `kimi provider` CLI (#313). IGNORE: TODO injection, MicroCompaction, ACP, background-ask, `/btw`, `/undo`, adaptive-thinking, flag logging, app_version metadata. See report 60 Part B.

- **2026-06-03 (v1.1.0 feature — first post-GA feature release)** — Shipped the **`/kimi:pursue`** autonomous goal-mode command + the `goal.summary` stream-json parser hardening, built on the 0.7–0.9 compat audit's feature track (report 60 Part B). `/kimi:pursue` exposes kimi-code 0.8.0+ headless goal mode as a write-capable, autonomous command that reuses the rescue trust boundary (same PreToolUse hook + workspace allowlist on every continuation turn, `command_type:"rescue"` lineage), bounded by a mandatory `--budget` wall-clock ceiling. Foreground-only, no `--resume` (goalId≠sessionId), experimental. Incorporated a kimi shape-consult (separate command, foreground-first, the goalId/sessionId trap) and a code-reviewer APPROVE (no write-/hook-bypass). The **load-bearing safety test** — a goal-mode real-binary smoke proving the hook fires on every continuation turn — was authored and validated GREEN against kimi-code 0.9.0 (zero files across a full wall-clock budget of relentless autonomous retries). Loop detection (#364) documented in `docs/safety.md` as a complementary autonomy guardrail. Still open: per-push CI for the goal-mode smoke (shares H7's OAuth-credentials-secret blocker); promoting pursue from `command_type:"rescue"` lineage to a first-class command type; `--background`/`--resume` once upstream goal mode stabilizes. Reports 56–60.

- **2026-06-09** — kimi-code shipped **four releases since the verified 0.9.0** (0.10.0, 0.10.1, 0.11.0, 0.12.0); the operator's binary had already auto-upgraded to 0.12.0 out-of-band (PR #334 drift — exactly the case the version probe was built to catch). Certified the whole jump in one cumulative 0.9.0→0.12.0 pass via the playbook's 4-reviewer audit (hook contract / stream-json / CLI surface / adversarial). Verdict: **COMPAT-PRESERVED** (unanimous), backed by a GREEN `bun run smoke:real` on the installed 0.12.0 binary (review/challenge/ask/review_gate all hook-denied — review_gate's first run aborted on the 120s default test budget, a timeout not a deny-miss, and passed on a 240s re-run; the pursue goal-mode smoke wrote zero files across a full budget). Patch release **v1.1.1** extends `KIMI_TESTED_MINORS` to `{0,10}`, `{0,11}`, `{0,12}` and tags `compat-verified-kimi-code-0.12.0`. Findings:
  - `03-hooks.diff` (both `agent/hooks/` + `session/hooks/`) is **0 bytes across all five tags** (0.9.0/0.10.0/0.10.1/0.11.0/0.12.0). Hook engine, `PreToolCallHookPermissionPolicy`, and the `PermissionManager` reducer byte-identical by blob SHA; `PreToolCallHookPermissionPolicy` still index 0 (auto-approve index 4). Stream-json writer (`writeResumeHint`/`PromptJsonWriter`) and the role-less `goal.summary` shape unchanged.
  - **Goal-mode experimental gate REMOVED in 0.12.0 (#569, commit d7407b0)** — the headline change. `run-prompt.ts` dropped `harness.getExperimentalFlags()`/the `flagEnabled` param, so `parseHeadlessGoalCreate(opts.prompt!)` now triggers headless goal mode on the `/^\/goal(\s|$)/` prompt prefix ALONE (the gate was still present 0.9.0–0.11.0; verified tag-by-tag). **This invalidates the old "double-gated" claim** in AGENTS.md / `kimi-version-probe.ts` / `docs/safety.md` / `pursue.ts` — corrected to single-gated this release. Compat-benign: the plugin never relied on the env gate — read-only commands hard-prefix an English instruction line (`buildAskPrompt`/`buildReviewPrompt`) so their trimmed prompt never starts with `/goal` (adversarial reviewer's `/kimi:ask "/goal …"` attack failed at this layer), and the index-0 hook denies every write regardless. `/kimi:pursue` still sets `KIMI_CODE_EXPERIMENTAL_GOAL_COMMAND=1` — now redundant on 0.12 but harmless (unknown experimental flag ids resolve to `undefined`) and still required on 0.8–0.11.
  - **AgentSwarm (#424)** — new `AgentSwarm` tool + `swarmMode` + a `SwarmModeAgentSwarmApprovePermissionPolicy` at queue index ~14 (below hook(0)/auto-approve(4)). Approve-only, double-guarded (`name==='AgentSwarm' && swarmMode.isActive`); `swarmMode.enter('tool')` runs INSIDE the tool's `execute()` — after the index-0 hook already gated the `AgentSwarm` call. Swarm subagents still inherit `DenyAllPermissionPolicy` (`subagent-host.ts:233`, byte-identical). Unreachable/benign for plugin spawns.
  - **`doctor` subcommand + `program.argument('[args...]')` unknown-positional error** — both unreachable: the plugin passes the prompt as the VALUE of `-p, --prompt`, never a bare positional. Auto-approve still hard-coded in `-p`.
  - **04-wire-records (108 KB)** — entirely on-disk session-store / record-replay / swarm-batching churn (`session/goal.ts` deleted −826, `subagent-batch.ts` +638, `records/types.ts` +51, session-store migration `v1.4.ts`). None reaches the `-p` stdout NDJSON surface the plugin parses.
  - Edits applied: `runtime/kimi-version-probe.ts` (`KIMI_TESTED_MINORS` → `{0,10}/{0,11}/{0,12}` + audit comment), `runtime/stream-json.ts` (verified-through widened to 0.12.0, goal.summary gate note corrected), AGENTS.md (Version, Upstream-compat paragraph, dual-source paragraph, pursue invariant), `docs/safety.md` (goal-mode gate-change note + verified-through), `runtime/commands/pursue.ts` (env-redundancy note + verified-through), this log entry, 5-file version bump 1.1.0 → 1.1.1.
  - Reports: `.claude/kimi-code-research/reports/61-upstream-012-hook-contract.md`, `62-…-stream-json.md`, `63-…-cli-surface.md`, `64-…-adversarial.md`, `65-…-synthesis.md`.
  - **Feature-track / MONITOR (NOT shipped in v1.1.1):** (1) **goal mode is no longer experimental-flagged (0.12.0)** — strengthens the case for promoting `/kimi:pursue` out of "experimental". (2) **PR #552 "restore goal resume state from agent records"** is the upstream work that may reconcile the goalId/sessionId split currently blocking pursue `--resume`; re-investigate whether headless goal resume is now feasible. (3) **AgentSwarm** (parallel subagent swarms) — not aligned with the plugin's read-only/rescue/pursue surface; IGNORE unless a concrete use case appears. (4) **`kimi doctor`** — could enrich `/kimi:setup` diagnostics; low priority.

- **2026-06-12** — kimi-code shipped **five releases since the verified 0.12.0** (0.12.1, 0.13.0, 0.13.1, 0.14.0, 0.14.1); the operator's binary had already auto-upgraded to 0.14.1 out-of-band (PR #334 drift — two minors past the verified max, firing the v1.2.1 "newer than tested max" probe warning, exactly the case it was built to surface). Certified the whole jump in one cumulative 0.12.0→0.14.1 pass via the playbook's 4-reviewer audit (hook contract / stream-json / CLI surface / adversarial). Verdict: **COMPAT-PRESERVED** (unanimous), backed by a GREEN `bun run smoke:real` on the installed 0.14.1 binary — review/challenge/ask/review_gate all hook-denied, the pursue goal-mode smoke wrote zero files across a multi-turn budget (goal.summary parsed cleanly: `turnsUsed:2, tokensUsed:69450, goalId:…`), and the swarm smoke confirmed a spawned swarm subagent's forced write is hook-denied. Patch release **v1.2.3** extends `KIMI_TESTED_MINORS` to `{0,13}`, `{0,14}` and tags `compat-verified-kimi-code-0.14.1`. Findings:
  - **Hook + permission chain intact.** Hook engine (`session/hooks/{engine,runner}.ts`), `pre-tool-call-hook.ts`, the stream-json writer (`PromptJsonWriter`/`writeResumeHint`), the `records/` dir, and CLI argv (`options.ts`/`commands.ts`) are all **byte-identical** 0.12.0→0.14.1 (verified per-tag to rule out add-then-revert). The only `session/hooks/` change is an additive `Interrupt` event type (`types.ts`, +1 line), inert for a PreToolUse-only consumer. `PreToolCallHookPermissionPolicy` is **still index 0** (`policies/index.ts:28-30`).
  - **Permission-stack churn (`02-permission.diff`, 6.3 KB) — two changes, both compat-benign.** (a) NEW `AgentSwarmExclusiveDenyPermissionPolicy` inserted at **index 1**: a pure DENY that fires only when a batch has >1 `AgentSwarm` or mixes `AgentSwarm` with other tools — "first non-undefined wins" means it can never pre-empt the index-0 hook, and a single lone `AgentSwarm` call (what `/kimi:swarm` issues) returns `undefined` and proceeds. It enforces "one `AgentSwarm` per response, alone in its batch" (also documented now in `agent-swarm.md`). (b) `CwdOutsideFileWriteAskPermissionPolicy` REMOVED: an `ask` policy that sat AFTER `auto-mode-approve`, so it was already dead in `-p` auto mode — its removal opens zero new write surface; the plugin owns workspace confinement via `rescue-approval.ts`, never kimi's cwd-ask. The index-1 insert shifts `AutoModeApprovePermissionPolicy` from index 4 (0.6.0–0.12.0) to **index 5** in 0.14.1 — structural invariant unchanged (every policy between the index-0 hook and the first approve is a DENY).
  - **AgentSwarm / goal-mode `-p` paths unchanged.** All 0.13/0.14 AgentSwarm + goal-mode churn is TUI-side (`tui/commands/`, dialogs, panels, markers); the AgentSwarm tool core (`agent-swarm.ts`, `name = 'AgentSwarm' as const`) and `subagent-host.ts` (swarm subagent standard permission stack — no `deny-all`) are byte-identical, and the headless `kimi -p "/goal …"` path is unchanged. The real-binary swarm + pursue smokes both passed.
  - **New `packages/protocol/` REST+WebSocket control API (PR #612, ~hundreds of lines, the headline 0.13/0.14 feature)** — a SEPARATE transport for remote/web control. Confirmed NOT on our `-p` path: `run-prompt.ts` imports none of `protocol`/`rpc/events`/`SDKSessionRPC`, and in stream-json mode the only stdout writer is `new PromptJsonWriter(stdout)` (`run-prompt.ts:384`). No change to the parsed NDJSON shape.
  - **Other 0.13/0.14 changes, all off our path:** session-lifecycle (active-turn cancel-on-close with 8s timeout + `BACKGROUND_KEEP_ALIVE_ON_EXIT` default flipped true→false — both ALIGN with our SIGTERM→SIGKILL descendant-reaping, neither orphans anything new), an `alwaysThinking` model-capability flag (read-only detection, relevant to but not unblocking H5), a `run-prompt.ts` SIGHUP cleanup handler (exit 129; SIGTERM still exits 143 and runs cleanup), and a new builtin `import-from-cc-codex` skill.
  - **Housekeeping:** restored sync-corrupted `dist/*.js` (an iCloud sync conflict under `~/Documents/` had rolled the working tree back to stale 1.2.0-era dist content and left `* 2.js` conflict copies; `git checkout -- dist/` + deleting the `* 2.js` artifacts restored the committed 1.2.2 dist before the rebuild). Not a code change — pre-existing working-tree damage cleaned up as part of the audit.
  - Edits applied: `runtime/kimi-version-probe.ts` (`KIMI_TESTED_MINORS` → `{0,13}/{0,14}` + audit comment, index-4→5 shift noted), `runtime/stream-json.ts` (verified-through widened to 0.14.1 + protocol-transport note), AGENTS.md (Version, Upstream-compat paragraph), this log entry, 5-file version bump 1.2.2 → 1.2.3.
  - Reports: `.claude/kimi-code-research/reports/72-upstream-0141-hook-contract.md`, `73-…-stream-json.md`, `74-…-cli-surface.md`, `75-…-adversarial.md`, `76-…-synthesis.md`.
  - **Feature-surface spike (report 77, 2026-06-12):** a dedicated pass confirmed **0.13/0.14 opened no new *buildable* feature surface** — every plugin-relevant change is dormant scaffolding, TUI-only, read-only detection, or passive cancellation hygiene. Both deferred features are unmoved by the cycle: **write-swarm's C1 blocker (per-subagent worktree isolation) is still unsolved upstream** (the AgentSwarm subagent path — `subagent-host.ts`/`subagent-batch.ts`/`agent-swarm.ts` — is byte-identical 0.12.0→0.14.1; `resume_agent_ids` is a continuation primitive, not isolation), and **pursue-`--resume` is STILL-BLOCKED** (no headless `/goal resume` re-drive landed). **Next-build recommendation (unchanged by this cycle):** report 71's **write-capable parallel swarm via plugin-orchestrated ephemeral git worktrees** (N `kimi -p` rescue jobs, one per worktree, merged by the main thread) — it needs **zero upstream change** and is the strongest candidate for the next minor (v1.3.0), independent of AgentSwarm's missing isolation.
  - **Feature-track / MONITOR (NOT shipped in v1.2.3):** (1) **`packages/protocol/` REST+WS control API (#612)** — a dormant remote/web-control daemon transport (no server binds, no `kimi serve` entry point); the one genuinely strategic item — a potential future transport beyond subprocess stream-json — but MONITOR until upstream wires it up, not buildable now. (2) **`Interrupt` hook event** — new hook event type; no current plugin use case. (3) **`alwaysThinking` capability** — upstream is adding thinking-capability *metadata* (read-only), still not the per-spawn thinking-control knob H5 wants; keep H5 MONITOR. (4) The 0.14 `AgentSwarmExclusiveDenyPermissionPolicy` means `/kimi:swarm` coordinators must issue swarms one-at-a-time, alone in a batch — already how a single fan-out behaves; note in swarm docs only if a coordinator ever trips the deny.

- **2026-06-13 (patch checkup, not a triggered multi-minor audit)** — kimi-code shipped **0.14.2** (npm `latest`, GitHub `Latest`); the operator's binary had already auto-upgraded into it out-of-band (PR #334 drift), but because v1.2.3 already listed `{0,14}` in `KIMI_TESTED_MINORS`, the version probe did **not** fire — this is a patch *within* an already-certified minor, so the playbook's full 4-reviewer audit is **not** warranted. Generated the four scoped diffs (`@moonshot-ai/kimi-code@0.14.1`..`0.14.2`) and read them in the main thread. Verdict: **COMPAT-PRESERVED, zero runtime change**, backed by a GREEN `bun run smoke:real` (7 pass / 0 fail) on the operator's installed **0.14.2** binary — review/challenge/ask/review_gate all hook-denied, the pursue goal-mode smoke wrote zero files across a multi-turn budget (`goal.summary` parsed cleanly: `turnsUsed:2, tokensUsed:96462, goalId:12cd8e38…`), and the swarm smoke confirmed a spawned swarm subagent's forced write is hook-denied. **No version bump, no 4-reviewer audit** — docs + a flaky-test de-flake only; tag `compat-verified-kimi-code-0.14.2`. Findings:
  - **The three load-bearing surfaces are 0-byte diffs.** `02-permission.diff` scoped to `permission/` alone is **0 bytes** (policy queue order unchanged — `PreToolCallHookPermissionPolicy` still index 0, `AgentSwarmExclusiveDenyPermissionPolicy` still index 1). `03-hooks.diff` (`agent/hooks/` + `session/hooks/`) is **0 bytes** (hook engine byte-identical). The stream-json writer surfaces — `records/` and the `PromptJsonWriter`/`writeResumeHint` region of `run-prompt.ts` — are **0 bytes** (NDJSON output shape, `session.resume_hint`, `goal.summary` all unchanged; smoke parsed `goal.summary` cleanly).
  - **The ~33-file uniform tool change is a bundler-import migration.** Nearly every `tools/builtin/**` file plus `session/subagent-host.ts` and `agent.ts` changed only their markdown-description imports: `from './x.md'` → `from './x.md?raw'` (a Vite raw-import suffix; one line in most files, three import lines in `agent.ts`, and `prompt-modules.d.ts` updated to `declare module '*?raw'`). Build-time only; no runtime behavior, no tool-name or approval-rule change. **`AgentSwarm` tool name is still `'AgentSwarm' as const`** (load-bearing for `/kimi:swarm`'s hook allow-list) and the singular `Agent` tool is still denied.
  - **`bash.ts` (+18/−8, the rescue execution surface) adds an `onUpdate` streaming callback** that emits stdout/stderr chunks live during a command. `approvalRule: literalRulePattern(this.name, args.command)` and `matchesRule` are unchanged, so the PreToolUse hook still receives the same `{tool_name:"Bash", tool_input:{command}}` shape and the rescue allowlist still parses `args.command` identically. The callback feeds TUI/RPC live output, not `-p` stdout (the stream-json writer is byte-identical), so our parser sees nothing new. Pure observability.
  - **`run-prompt.ts` (+3) writes config-diagnostics warnings to STDERR** (`for (const warning of (await harness.getConfigDiagnostics()).warnings) stderr.write(\`Warning: ${warning}\n\`)`). stderr is humans-only on our side; the stdout stream-json parser is unaffected, and the strict line-anchored stderr resume-hint fallback regex (full-UUID payload required) cannot misread a `Warning:` line. This stderr line is the only `-p`-reachable surface of 0.14.2's new config-diagnostics subsystem (a ~180-line `config/toml.ts` addition — `readConfigFileForUpdate`/`TomlError` — plus `getConfigDiagnostics`/`ConfigDiagnostics` on the RPC core and SDK harness); the rest of that subsystem, and the accompanying TUI/rpc/skill churn, is off our subprocess path — the plugin owns config writes via its own `writeConfigAtomic` and never calls `getConfigDiagnostics`/`readConfigFileForUpdate`.
  - **`options.ts` (−9) removes three `!promptMode`-gated CLI conflict checks** (`--yolo`/`--auto`/`--plan` × `--continue`/`--session`). All were guarded by `!promptMode`, so they only ever fired in *shell* mode — dead in our `-p` path. Removal is benign for the wrapper.
  - **No new buildable feature surface.** 0.14.2 is a maintenance patch; report 77's next-build recommendation (write-capable parallel swarm via plugin-orchestrated ephemeral git worktrees, zero upstream change, v1.3.0 candidate) is unchanged, and the write-swarm C1 (per-subagent worktree isolation) + pursue-`--resume` blockers remain upstream-unmoved.
  - **Gap fixed (not upstream-related): flaky cancellation test de-flaked.** `cli-client.test.ts` › "escalationMs: Infinity opts out of SIGKILL" failed ~1-in-5 under full-suite parallel load. Root cause (confirmed by reading `cli-client.ts`): `result.aborted` is set only inside `onAbort()`; the test fired `controller.abort()` on a nominal-100ms `setTimeout` that competed, across two processes, with the stub's 500ms self-exit timer. Under event-loop starvation the 100ms abort was delayed past the stub's clean self-exit, so `onAbort` never ran and `aborted` stayed `false`. Fix: abort deterministically from `onRecord` (the child's first record proves it is alive; `invokeOnRecord` fires only from the stdout data handler, attached *after* the abort listener), making the ordering causal instead of a timer race. The runtime was correct throughout (with `Infinity` it never sends SIGKILL) — this was purely a test-margin bug. Verified 20/20 in isolation and 4/4 + a full green `bun run check` under 16-worker CPU contention.
  - Edits applied: `runtime/kimi-version-probe.ts` (0.14.2 audit comment after `{0,14}`; no array change — minor-level membership already covers it), `runtime/stream-json.ts` (verified-through widened to 0.14.2), AGENTS.md (Version line + Upstream-compat paragraph), `tests/runtime/cli-client.test.ts` (deterministic abort), this log entry, rebuilt `dist/`. No 5-file version bump.

- **2026-06-14 (patch checkup, not a triggered multi-minor audit)** — kimi-code shipped **0.14.3** (npm `latest`, GitHub `Latest`); the operator's binary had already auto-upgraded into it out-of-band (PR #334 drift), but because `{0,14}` is already in `KIMI_TESTED_MINORS`, the version probe did **not** fire — a patch *within* an already-certified minor, so the playbook's full 4-reviewer audit is **not** warranted. Generated the four scoped diffs (`@moonshot-ai/kimi-code@0.14.2`..`0.14.3`) and read them in the main thread. Verdict: **COMPAT-PRESERVED, zero runtime change**, backed by a GREEN `bun run smoke:real` (7 pass / 0 fail) on the operator's installed **0.14.3** binary — review/challenge/ask/review_gate all hook-denied, the pursue goal-mode smoke wrote zero files across a multi-turn budget (`goal.summary` parsed cleanly: `turnsUsed:2, tokensUsed:49428, goalId:f9738c6f…`), and the swarm smoke confirmed a spawned swarm subagent's forced write is hook-denied. **No version bump, no 4-reviewer audit** — docs-only; tag `compat-verified-kimi-code-0.14.3`. Findings:
  - **All four scoped diffs are 0-byte.** `01-cli-prompt-mode.diff` (`run-prompt.ts` + `options.ts`/`commands.ts`), `02-permission.diff` (`permission/` — policy queue order unchanged, `PreToolCallHookPermissionPolicy` still index 0, `AgentSwarmExclusiveDenyPermissionPolicy` still index 1), `03-hooks.diff` (`agent/hooks/` + `session/hooks/` — hook engine byte-identical), and `04-wire-records.diff` (`records/` + `session/` — NDJSON output shape, `session.resume_hint`, `goal.summary` all unchanged) are **each 0 bytes**. Every surface the safety model rests on is byte-identical 0.14.2→0.14.3.
  - **The entire ~300-line patch is a single TUI change (PR #713 — "Refresh provider model metadata before opening the model picker").** `tui/commands/config.ts` (+36): `handleModelCommand` is now `async` and calls a new `refreshModelsForPicker()` (wrapping `host.authFlow.refreshOAuthProviderModels()` in a 2s `withTimeout`) before `showModelPicker`. `tui/commands/dispatch.ts` (+1): `await handleModelCommand`. `tui/controllers/auth-flow.ts` (+25/−19): factors `refreshProviderModels` into a scoped `refreshProviderModelsWithScope('all'|'oauth')` and adds the `oauth` variant. `tui/utils/refresh-providers.ts` (+16): `refreshAllProviderModels` gains an optional `{scope}` param that early-returns after the OAuth-provider pass when `scope==='oauth'`. Plus the matching TUI test files and the EN/ZH changelog.
  - **None of it reaches the `-p` headless path.** The changed `/model` command lives in the interactive TUI slash dispatcher (`tui/commands/`), not the `-p` argv parser (`cli/commands.ts`/`options.ts`, both 0-byte). `refreshAllProviderModels` is invoked only from the TUI auth-flow controller; the sole reference outside `tui/` is a JSDoc mention in `cli/sub/provider.ts` (the `kimi provider` subcommand the plugin never invokes) that doesn't call it, and the new `scope` param is optional, **defaulting to `'all'`**, so existing callers are unaffected. `run-prompt.ts` is byte-identical, so its pre-existing `oauth_refresh` telemetry call is unchanged. The config-writing `setConfig`/`removeProvider` calls inside `refresh-providers` fire only from the TUI auth-flow controller, never from a `-p` spawn.
  - **No new buildable feature surface.** 0.14.3 is a TUI-only maintenance patch; report 77's next-build recommendation (write-capable parallel swarm via plugin-orchestrated ephemeral git worktrees, zero upstream change, v1.3.0 candidate) is unchanged, and the write-swarm C1 (per-subagent worktree isolation) + pursue-`--resume` blockers remain upstream-unmoved.
  - Edits applied: `runtime/kimi-version-probe.ts` (0.14.3 audit comment after `{0,14}`; no array change — minor-level membership already covers it), `runtime/stream-json.ts` (verified-through widened to 0.14.3), AGENTS.md (Version line + Upstream-compat paragraph), this log entry, rebuilt `dist/` (the comment-only `.ts` edits carry through to `dist/kimi-version-probe.js` + `dist/stream-json.js` — bun preserves leading comments — so the drift gate required a rebuild; verified comment-only, no executable code changed). No 5-file version bump.

- **2026-06-16 (new-minor checkup → v1.2.4 release)** — kimi-code shipped **0.15.0** (npm `latest`, GitHub `Latest`); the operator's binary had auto-upgraded into it out-of-band (PR #334 drift), and crossing the 0.14→0.15 minor boundary fired the `/kimi:setup` version probe's H9 "newer than tested max" warning (`KIMI_TESTED_MINORS` covered `{0,…,14}`, not `{0,15}`). The daily scheduled routine produced a **propose-only** surface review (report 78) + a Phase-1b smoke; this entry records the human-applied catch-up. Verdict: **COMPAT-PRESERVED** — extending the tested-minors array is the only runtime change. The report's verdict was **re-confirmed independently before tagging** via `git diff … | wc -c` on the local clone (`@moonshot-ai/kimi-code@0.14.3..0.15.0`), not taken on trust. Backed by a GREEN `bun run smoke:real` on the operator's installed **0.15.0** binary (report 78: 7 pass / 0 fail; review/challenge/ask/review_gate hook-denied; pursue goal-mode wrote zero files, `goal.summary` parsed cleanly `turnsUsed:2, tokensUsed:51199, goalId:47044d44…`; swarm subagent write hook-denied). **Real patch bump (v1.2.4)** — unlike the docs-only 0.14.2/0.14.3 patches, extending `KIMI_TESTED_MINORS` is a runtime change. Findings:
  - **The load-bearing safety surfaces are 0-byte.** `permission/` (PreToolCallHookPermissionPolicy still index 0, AgentSwarmExclusiveDenyPermissionPolicy still index 1), the hook engine (`agent`+`session` `hooks/`), and `run-prompt.ts` + CLI argv (`commands.ts`/`options.ts`) are **each 0 bytes** between 0.14.3 and 0.15.0 — confirmed by direct byte-count, not just the report.
  - **The only changed scoped surface is `records/` + `session/` (~5.3 KB), all off the `-p` path.** PR #786 drops `app_version`/`resumed` from the persisted `.records/` metadata replay artifact (NOT the `-p` stdout stream — `PromptJsonWriter` is byte-identical); PR #784 renames `SkillRegistry`→`SessionSkillRegistry`; PR #776 swaps a temporary-provider instantiation for a static model-capability lookup. `runtime/stream-json.ts` only reads role-keyed records (`assistant`, `meta`/`session.resume_hint`, `goal.summary`) and never consumed `metadata`. Benign.
  - **MCP SSE transport (#744) is additive config only.** A new `transport:'sse'` variant in the MCP config discriminated union (`config/schema.ts`); the plugin configures no MCP servers for its `-p` spawns and `writeConfigAtomic` preserves the rest of the TOML, so it cannot break our written config.
  - **Everything else is TUI / system-prompt / packaging churn** (all-sessions session picker #779, same-language status-sentence prompt wording #780/#787, staged auto-update rollout #775) — off the `-p` path. `AgentSwarm` tool name unchanged; `tool/index.ts` re-registration is transparent to our name-gated hook.
  - Edits applied: `runtime/kimi-version-probe.ts` (`{0,15}` added to `KIMI_TESTED_MINORS` + audit comment — the load-bearing change), `runtime/stream-json.ts` (verified-through widened to 0.15.0), AGENTS.md (Version line + Upstream-compat paragraph + Releasing list + Post-GA H7 framing), `docs/safety.md` (corrected a stale `auto-mode-approve at index 4` → index 5 parenthetical, drifted since 0.14.1), `CHANGELOG.md` (revived with a post-GA pointer banner + 1.2.4 entry), `CONTRIBUTING.md`/`.github/workflows/smoke.yml`/`docs/ci.md` doc-currency fixes, `docs/upstream-compat-audit.md` (playbook diff-scope correction), this log entry, 6-file version bump 1.2.3 → 1.2.4 (now incl. CHANGELOG.md per the updated Releasing list), rebuilt `dist/`. Tags `v1.2.4` + `compat-verified-kimi-code-0.15.0`.
  - Report: `.claude/kimi-code-research/reports/78-upstream-0150-surface.md`.

- **2026-06-17 (new-minor checkup → v1.2.5 release)** — kimi-code shipped **0.16.0**; the operator's binary auto-upgraded 0.15.0→0.16.0 within ~a day (PR #334 drift), re-firing the `/kimi:setup` H9 "newer than tested max" warning one minor after v1.2.4 silenced it for 0.15.x. Verdict: **COMPAT-PRESERVED** — extending `KIMI_TESTED_MINORS` to `{0,16}` is the only runtime change. Verified independently via `git diff` byte-count on the clone (`@moonshot-ai/kimi-code@0.15.0..0.16.0`) and a **GREEN** `bun run smoke:real` on the operator's installed **0.16.0** binary (7 pass / 0 fail; review/challenge/ask/review_gate hook-denied; pursue goal-mode wrote zero files, `goal.summary` parsed cleanly `turnsUsed:2, tokensUsed:52211, goalId:fd944733…`; swarm subagent write hook-denied). Findings:
  - **The 3 core safety surfaces are 0-byte.** `permission/` (PreToolCallHookPermissionPolicy still index 0, AgentSwarmExclusiveDenyPermissionPolicy still index 1), the hook engine (`agent`+`session` `hooks/`), and `run-prompt.ts` are each 0 bytes 0.15.0→0.16.0.
  - **The only CLI argv change is a new `kimi vis` subcommand** (a visualization server; `commands.ts` registers it + a new `cli/sub/vis.ts`). Off the `-p` path — our `-p`/`-r`/`--output-format`/`-m`/`--skills-dir` flags are untouched and auto-approve is unchanged. The plugin never invokes `kimi vis`.
  - **The records/session/replay/agent churn (~834 insertions) is internal, off the `-p` stdout stream.** A compaction refactor (`compaction/full.ts`, `strategy.ts`), a new `llm-request-logger.ts`, replay-build additions (`replay/build.ts`, `replay/index.ts`), a `SkillRegistry`→`SessionSkillRegistry` continuation, and a `kimi-env-params.ts` move (`agent/`→`config/`). `runtime/stream-json.ts` reads only the role-keyed `-p` stdout records (`PromptJsonWriter`, byte-identical) — none of this reaches it. `utils/paths.ts` adds only a TUI `cache/banner/` dir (not a creds/home layout change).
  - **Process note (candidate hardening item):** the operator's re-login (to refresh an expired `managed:kimi-code` OAuth token mid-audit) made kimi-code rewrite `config.toml`, which **stripped the plugin's managed-block comment markers** (`# === BEGIN/END kimi-plugin-cc-managed ===`) — kimi-code's TOML writer doesn't preserve comments. The `[[hooks]]` entry and the hook's enforcement survived; only the markers were lost, degrading `/kimi:setup`'s marker-based idempotency/verification (a marker-less block isn't recognized, so a later setup could add a duplicate, and `--check` may not find it). Re-running `/kimi:setup` restores the block. Hardening candidate: make the verifier match a marker-less-but-byte-correct hook entry, or detect+repair a stripped block.
  - Edits applied: `runtime/kimi-version-probe.ts` (`{0,16}` + audit comment), `runtime/stream-json.ts` (verified-through 0.16.0), AGENTS.md (Version + Upstream-compat), CHANGELOG.md (1.2.5 entry), this log entry, 6-file version bump 1.2.4 → 1.2.5, rebuilt `dist/`. Tags `v1.2.5` + `compat-verified-kimi-code-0.16.0`.

- **2026-06-19 (new-minor catch-up + feature → v1.2.6, run from a CLOUD session)** — kimi-code shipped **0.17.0, 0.17.1, 0.18.0** since the verified 0.16.0 (npm `latest` = 0.18.0); the operator's binary auto-upgraded past the tested 0.16 max (PR #334 drift), re-firing the H9 "newer than tested max" probe warning. Ran the playbook's Phase 0 scoped diffs (`@moonshot-ai/kimi-code@0.16.0..0.18.0`) and read them in the main thread. Verdict: **COMPAT-PRESERVED** — extending `KIMI_TESTED_MINORS` to `{0,17}` + `{0,18}` plus the swarm-concurrency feature are the runtime changes. Findings:
  - **The core safety surfaces hold.** `03-hooks.diff` is **0-byte** (both `agent/hooks/` and `session/hooks/`) and `pre-tool-call-hook.ts` is 0-byte 0.16.0→0.18.0. `PreToolCallHookPermissionPolicy` is **still index 0**, `AgentSwarmExclusiveDenyPermissionPolicy` index 1, `AutoModeApprovePermissionPolicy` index 5 (first approve; every policy between the index-0 hook and it is a DENY). The ONLY permission change is a NEW `GoalStartReviewAskPermissionPolicy` (#839 guided goal authoring) inserted at index ~10 *after* auto-mode-approve — an `ask` gated to a *model-issued* `CreateGoal` in NON-auto mode that returns early when `permission.mode === 'auto'` (triple-dead on the `-p` auto path; an `ask` cannot approve). `options.ts` is **byte-identical**; `run-prompt.ts` changes only in a telemetry refactor (`started` event → harness `sessionStartedProperties`), with the permission-forcing chain and stream-json writer unchanged.
  - **"Kimi web" landed upstream and is off our path.** PR #625 ("Kimi web app + daemon gateway") adds a `kimi server`/`kimi web` subcommand stack and session ARCHIVE (`archived` flag + `archive()`/`includeArchive` in the session store + rpc core-api). The plugin owns its own SQLite job store, never invokes `kimi server`, and never reads kimi's session list, so neither reaches the `-p` path. (The operator flagged Kimi web's availability; confirmed benign — we dropped web-based session renaming long ago.)
  - **New buildable feature shipped: AgentSwarm concurrency cap (#888, 0.18.0).** `KIMI_CODE_AGENT_SWARM_MAX_CONCURRENCY` caps how many AgentSwarm subagents `SubagentBatch` runs concurrently (unset = legacy uncapped ramp; non-positive-integer throws). v1.2.6 wires it into `/kimi:swarm` so `--cap N` becomes a HARD concurrency ceiling on 0.18+ (via `CliClientOptions.swarmMaxConcurrency` → `cli-client.ts::buildEnv`), in addition to the existing soft prompt-count hint; older binaries ignore the unknown env var. This is the first new buildable swarm surface since v1.2.0 — write-swarm's C1 blocker (per-subagent worktree isolation) and pursue-`--resume` remain upstream-blocked.
  - **Phase 1b smoke — GREEN, cleared before merge.** The cloud session that *prepared* this catch-up had no kimi binary and no authenticated `~/.kimi-code`, so its `bun run smoke:real` was deferred to an explicit pre-merge gate (every prior `KIMI_TESTED_MINORS` extension cited a GREEN smoke). The gate was closed locally on **2026-06-19** against the operator's installed **0.18.0** binary: **7 pass / 0 fail** — review/challenge/ask/review_gate forced writes hook-denied; pursue's multi-turn goal wrote zero files (`goal.summary` parsed cleanly `turnsUsed:2, tokensUsed:44468`); a spawned swarm subagent's forced write hook-denied. The Phase 0 scoped diffs were **independently re-reproduced** before merge (`03-hooks`/`pre-tool-call-hook`/`options.ts` all 0-byte by `git diff | wc -c`; policy order: hook index 0, first approve index 5, GoalStartReviewAsk index ~10), and a **six-reviewer + independent adversarial** pass (the playbook's Phase 1 + Phase 4) all returned **CONFIRMED-SAFE**, including a code review of the new `--cap` → `KIMI_CODE_AGENT_SWARM_MAX_CONCURRENCY` wiring (the one surface not covered by report 81's smoke): a 19-edge-input round-trip proved every `--cap` the plugin accepts passes upstream's identical positive-integer validator, with zero new write/permission surface. Tag `compat-verified-kimi-code-0.18.0`.
  - **`/kimi:ask` shape consult (operator practice for surface changes).** Dogfooded a `/kimi:ask` on the `--cap` overload. Kimi recommended **splitting** the control — keep `--cap` as the soft total-count hint, add a separate `--max-concurrency` for the hard ceiling — flagging the "cap-as-total-count illusion": a user reading `--cap 5` (and the injected prompt's "≤5 subagents") expects a total-cost bound, but on 0.18+ the value only bounds *peak concurrency*; a coordinator can still launch many subagents *sequentially*. Shipped v1.2.6 with the overloaded `--cap` (honestly documented as concurrency≠total in `parsing.ts` + CHANGELOG; the always-on `--budget` wall-clock ceiling remains the real cost backstop). **Followed up immediately in v1.2.7 (2026-06-19, same day):** split into a soft `--cap` (total-count prompt hint) + a hard `--max-concurrency` (the env-var ceiling), so the two bounds are independently expressible and the illusion is gone — see the v1.2.7 entry below.
  - **Docs slimming (operator request).** Trimmed the AGENTS.md "Version" + "Upstream compat" lines from their accumulated per-version essays to a lean current-state summary + pointers, relocating the historical narrative to CHANGELOG.md and this log (the per-version compat reasoning also lives verbatim in `runtime/kimi-version-probe.ts` comments). Goal: keep AGENTS.md / CLAUDE.md readable for agents, with traces in the changelog/roadmap.
  - Edits applied: `runtime/kimi-version-probe.ts` (`{0,17}`+`{0,18}` + audit comment), `runtime/cli-client.ts` (`swarmMaxConcurrency` option + `buildEnv` overlay + spawn-log field), `runtime/commands/swarm.ts` (thread `--cap`), `runtime/parsing.ts` (two-tier `--cap` doc), `tests/runtime/cli-client.test.ts` + `tests/helpers/mock-kimi-stream.ts` (env-reach test), AGENTS.md (slimmed Version + Upstream-compat + swarm invariant), `docs/safety.md` (swarm cost bullet), CHANGELOG.md (1.2.6 entry), this log entry, 4-file version bump 1.2.5 → 1.2.6, rebuilt `dist/`. Prepared on branch `claude/bold-archimedes-s07l9l` (PR #10); the local-flow follow-up (GREEN smoke + Phase 0 re-reproduction + six-reviewer/adversarial pass + `/kimi:ask` shape consult, and flipping the smoke-pending docs to GREEN) was committed onto the branch, then fast-forwarded to `main` and tagged `v1.2.6` + `compat-verified-kimi-code-0.18.0`, `gh release` cut.

- **2026-06-19 (same-day fast-follow → v1.2.7)** — Acted on the `/kimi:ask` shape consult from the v1.2.6 entry: **split `/kimi:swarm`'s `--cap` overload** into a SOFT `--cap` (total-subagent-count prompt hint, unchanged behavior) + a NEW HARD `--max-concurrency` (the `KIMI_CODE_AGENT_SWARM_MAX_CONCURRENCY` ceiling on 0.18+). v1.2.6 had wired one `--cap` value to both, which Kimi flagged as the **"cap-as-total-count illusion"** (a user reading `--cap N` expects a total-cost bound but the env var only caps *peak concurrency*; sequential fan-out can still exceed it). Findings:
  - **No compat change, no new write surface.** `--max-concurrency` only sets a concurrency-bounding env var; the `swarm` hook label and read-only allowlist are untouched (PR touches no hook/policy/allowlist file). The split is purely which flag feeds `CliClientOptions.swarmMaxConcurrency` (now `--max-concurrency`, was `--cap`); `cli-client.ts::buildEnv` is unchanged, so the `cli-client.test.ts` env-reach tests still pass as-is.
  - **Validation preserved.** `--cap` and `--max-concurrency` share a new `parsePositiveIntFlag` helper, so both apply the identical `Number.isInteger && > 0` predicate that upstream's `resolveSwarmMaxConcurrency` requires — every accepted value still round-trips through `String()` without throwing.
  - **Why a patch (v1.2.7), not a minor:** a fast-follow refinement of a surface introduced in the immediately-prior patch (v1.2.6, same day), on an experimental prototype command — not a new capability. Backward-compat is a non-issue (v1.2.6's `--cap`-as-hard-cap shipped hours earlier on an experimental command; ~zero real-world use).
  - Edits applied: `runtime/parsing.ts` (`SwarmArgs.maxConcurrency` + `--max-concurrency` case + `parsePositiveIntFlag` helper + reverted `--cap` doc to soft-only), `runtime/commands/swarm.ts` (thread `--max-concurrency`, not `--cap`, into the env; scope/prompt/usage comments), `runtime/cli-client.ts` (field doc), `tests/runtime/swarm.test.ts` (split assertions), `commands/swarm.md` + AGENTS.md (Version + swarm invariant) + `docs/safety.md` (swarm cost bullet), CHANGELOG.md (1.2.7 entry), this log entry, 4-file version bump 1.2.6 → 1.2.7, rebuilt `dist/`. `bun run check` green; reviewed before tag.

- **2026-06-20 (forward-scan, not a triggered audit)** — Daily kimi-code checkup: npm `latest` / GitHub `Latest` / the operator's local binary are all **0.18.0** (= tested max; H9 probe silent). `origin/main` (`7644f10`) is 2 UNRELEASED commits past the `@moonshot-ai/kimi-code@0.18.0` tag — #898 (changelog docs) and #910 ("drop empty text blocks during projection"), an INPUT-side `agent/context/projector.ts` fix that is off the `-p` stdout stream our parser reads and actually HARDENS the `kimi -r` resume path. All four scoped diffs (`0.18.0`..`origin/main`) are **0-byte**. The sole pending `.changeset` is `patch` → next release is **0.18.1**, within the already-certified `{0,18}` minor (no probe fire, no `KIMI_TESTED_MINORS` change; a docs-only patch-checkup + GREEN smoke will suffice). Provisional **COMPAT-PRESERVED**. Report: `.claude/kimi-code-research/reports/82-forward-scan-post-0180.md`.

- **2026-06-20 (feature release → v1.3.0)** — Completed the command↔agent mirror: added model-invocable **`kimi-swarm`** (read-only parallel fan-out) and **`kimi-pursue`** (write-capable autonomous goal mode) subagents, so the main Claude thread can auto-dispatch every capability (previously only review/challenge/ask/rescue had agents; swarm/pursue were human-only slash commands). Auto-dispatch widens **no** safety surface — the index-0 PreToolUse hook fires on every tool call of every turn for all agents (read-only labels deny writes; rescue/pursue apply the workspace allowlist + no git mutation), and the heavy modes keep mandatory `--budget`/concurrency ceilings. **One runtime change:** `/kimi:swarm --max-concurrency` now defaults to **4** (`resolveSwarmMaxConcurrency`, `swarm.ts`) so a now-auto-dispatchable fan-out is peak-bounded by construction on kimi-code 0.18+ (was: kimi-code's internal ramp). Each agent passed its own 4-lens adversarial review workflow (contract / convention / auto-invocation-safety / doc-coherence) with must-fixes applied — swarm: tightened triggering + restricted backgrounding; pursue: fixed a propagated `blocked`-vs-budget factual bug (budget expiry is a `RESPONSE_TIMEOUT` failure, not the exit-3 `blocked` status) in both the agent and `commands/pursue.md`, prohibited backgrounding, budget-steers-down for auto-dispatch, and tightened the rescue↔pursue routing boundary. `bun run check` green; 6-file version bump 1.2.7 → 1.3.0; tag `v1.3.0`.
  - **Write-swarm reframed — supersedes the report-71 / report-77 "per-subagent ephemeral worktree" next-build pointer above.** That framing does not fit kimi-code: `AgentSwarm` subagents share the coordinator cwd (`packages/agent-core/src/session/subagent-host.ts:362`, `cwd: parent.config.cwd`), so per-subagent isolation is impossible without abandoning `AgentSwarm` (the very tool the swarm is built on). The chosen design (**A**) is **one ephemeral worktree for the whole swarm** — trust `AgentSwarm`'s in-process fan-out; the rescue allowlist confines writes to the worktree cwd (double-bound: can't escape the worktree, and the worktree is disposable); the main thread merges a captured patch (it still owns git). Per-subagent worktrees (Design B) are deferred to "if disjoint-target partitioning proves insufficient." Drafted spec (local): `.claude/docs/write-swarm-spec.md`. **v1.4 candidate.** → **shipped v1.4.0 (below).**

- **2026-06-20 (feature release → v1.4.0)** — Shipped **write-capable swarm** (`/kimi:swarm --write`), Design A. The coordinator + `coder` subagents run in an **ephemeral detached worktree off HEAD** (under the plugin data dir, never in the user repo); writes confined by a new `swarm-write` hook label to a **forge-proof trusted worktree root** (`KIMI_PLUGIN_CC_WORKSPACE_ROOT` env, not the payload cwd; fail-closed) reusing `evaluateRescueHookRequest` verbatim; result captured as an untruncated applyable `.patch` (`git add -N` + `git diff --binary`, no loose objects) on every terminal path before the worktree is removed; **the plugin never applies/commits — main thread owns the merge**. Gated to kimi-code **≥ 0.18.0** (hard concurrency cap), a git repo with a born HEAD, and the hook (refuses without it); default `--max-concurrency` **1** for write (vs 4 read); dirty-tree warning; **human-only** (no `kimi-swarm-write` agent). Preceded by a **3-way adversarial spec review** (safety / kimi-contract / convention lenses) whose two reviewer *blockers* were resolved by direct verification against the vendored kimi-code 0.18.0 source rather than taken on faith: (1) the in-process subagent's hook-payload `cwd` equals the coordinator spawn cwd (one `HookEngine` per session built with `cwd: kaos.getcwd()`, `session/index.ts:178`), and (2) swarm subagents use the **standard** permission stack — no deny-all (the lone `DenyAllPermissionPolicy.unshift` is `startBtw()`-only, NOT the swarm spawn path) — so `coder` writes actually land and are gated solely by our hook. Also confirmed: `coder` write tools (`Write`/`Edit`/`Bash`) map to existing allowlist branches (no new branch); `KIMI_CODE_AGENT_SWARM_MAX_CONCURRENCY` is per-`SubagentBatch` (nesting can multiply peak → prompt forbids nested swarm). Fixed a stale `kimi-version-probe.ts` comment that mis-cited swarm subagents as inheriting deny-all. Three open questions resolved with unanimous reviewer consensus: write concurrency default **1**, **present** the patch (no auto-apply), keep **human-only**. New unit tests (parse + write-concurrency-default + write prompt; swarm-write hook label incl. the "confine to trusted env root not payload cwd" + fail-closed cases; real-repo worktree create→capture-applyable-patch→remove with main tree untouched). `bun run check` green; 6-file version bump 1.3.0 → 1.4.0; tag `v1.4.0`. **Remaining field-proof:** a gated real-binary write-swarm smoke (write-lands-in-worktree-only, user-tree-unchanged) before any concurrency raise or `kimi-swarm-write` agent. Spec promoted with a "Review resolutions" section (local: `.claude/docs/write-swarm-spec.md`).

- **2026-06-23 (patch release → v1.5.1)** — Daily kimi-code checkup escalated to a certification: npm `latest` reached **0.19.1** (two releases past the tested 0.18 max — the operator's binary had auto-upgraded 0.18.0→0.19.1 out-of-band, PR #334 drift, firing the H9 "newer than tested max" probe warning). Verdict **COMPAT-PRESERVED**; extended `KIMI_TESTED_MINORS` with `{0,19}` (the sole runtime change → patch release). The 0.18→0.19 minor was bumped by **#812 "workspace add-dir support"** (commit `c0eeca2`): a new repeatable `--add-dir <dir>` flag + `GitCwdWriteApprovePermissionPolicy` widened to approve writes within cwd **OR** any additional dir. Compat-benign for the plugin: the **only** permission consumer of `additionalDirs` is `GitCwdWriteApprovePermissionPolicy`, which sits at **index 17** (below `AutoModeApprovePermissionPolicy` at index 5), so on the `-p` auto path it is structurally unreachable; read-only commands deny all writes at the index-0 hook; and our write commands confine to a single root via `rescue-approval.ts`, which never reads `additionalDirs`. (`isWithinWorkspace` with an empty list is byte-for-byte the old `isWithinDirectory(cwd)`, `path-access.ts:159-163`, covering the non-`-p` modes where the policy actually runs.) **Correction caught in the Phase-4 adversarial review:** `additionalDirs` is **not** guaranteed empty for a plugin `-p` spawn even without `--add-dir` — #812 also wired an unconditional auto-load of project-local `.kimi-code/local.toml` `additional_dir` into BOTH the `-p` create and resume bootstraps (`rpc/core-impl.ts:238,363`, via `config/workspace-local.ts`; both absent at 0.18.0) — but per the above it changes no approve/deny decision on our path. Safety chain re-verified intact: `03-hooks.diff` 0-byte (both `agent/hooks/` + `session/hooks/`); `policies/index.ts` byte-identical (`PreToolCallHookPermissionPolicy` index 0, `AutoModeApprovePermissionPolicy` first approve at index 5); `permission:'auto'` still hard-coded. Off-path: **#963** (`4292ae9`) adds a `turn.ended reason:'filtered'` (provider content filter) — a failure *reason*, not a new stdout *shape*, so the role-keyed stream-json parser is unaffected; #821 (detach-to-background) was already triaged in the forward-scans; 0.19.1 over 0.19.0 is `ci:`/`fix(acp)`/`fix(web)` only (0-byte load-bearing diff). **Backed by a GREEN `bun run smoke:real` (9 pass / 0 fail)** on the operator's 0.19.1 binary — review/challenge/ask/review_gate forced writes hook-denied; pursue multi-turn goal wrote zero files (`goal.summary` parsed cleanly, turnsUsed:2 tokensUsed:73631); a read-only swarm subagent's forced write hook-denied; and **both write-swarm assertions held** (a `coder` subagent's edits confined to the throwaway worktree, patchBytes=306, `userTreeClean=true`; an out-of-trusted-root absolute write hook-denied) — the exact surface the `add-dir` widening theoretically threatened, empirically tight. **Next-audit note:** #812 (0.19.0) — **not** #992 — made the `.kimi-code/local.toml` `additional_dir` auto-load LIVE on the `-p` path for all transports (#992 only re-routed that read's kaos handle for an ACP fix). The "never passes `--add-dir` ⟹ `additionalDirs` empty" assumption is broken upstream; re-confirm each audit that the index-0 hook still pre-empts `GitCwdWriteApprove`, and consider a defensive assert/scrub if a future minor lets `additionalDirs` reach a policy above our hook. `bun run check` green; 6-file version bump 1.5.0 → 1.5.1; tags `v1.5.1` and `compat-verified-kimi-code-0.19.1`. Surface report: `.claude/kimi-code-research/reports/85-upstream-0191-surface.md`.

- **2026-06-29 (daily checkup, no release tag)** — kimi-code 0.20.0 → 0.20.2 (patches; the 0.20.2 patch report 90 forward-scanned has landed and is the operator's live binary, auto-upgraded via PR #334 drift). Verdict **COMPAT-PRESERVED** on the actual released `0.20.2` tag (`52e2719`). **No version bump / no `KIMI_TESTED_MINORS` change** — 0.20.2 ∈ already-tested `{0,20}`; H9 silent (a green-smoke verification of an already-tested minor, recorded with a `compat-verified-kimi-code-0.20.2` marker tag, not a plugin release). Load-bearing surfaces clean: `02-permission.diff` 0-byte (`PreToolCallHookPermissionPolicy` still index 0, `AutoModeApprove` index 5); `agent/records/` 0-byte (stream-json shape unchanged); `run-prompt.ts` permission/writer chain unchanged (only telemetry-property renames, #1184). **New load-bearing surface (the finding): #1127 "support hooks in plugins" (0.20.1)** — `rpc/core-impl.ts` create+resume now merge enabled kimi-code *plugin* manifest hooks into the `-p` session hook list (`[...config.hooks, ...plugins.enabledHooks()]`), so we are no longer the sole occupant of the PreToolUse channel. Compat-benign: engine aggregation is **any-block-wins** (`session/hooks/engine.ts::blockDecision` = first `action:'block'` wins; an allow never pre-empts a block) → a plugin hook can only *add* denials, never override our managed deny; our config.toml managed block is unaffected (config `HookDefSchema` is `.strict()`, so the new per-hook `cwd`/`env` are plugin-only — no config-injection). Hook **aggregation semantics** is now an explicit audit-checklist item (re-verify `blockDecision` stays find-first-block). Off-path: #1170/#1186 Anthropic-protocol/betaApi transport plumbing (config `protocol`/`betaApi` alias fields + `provider-manager.ts`; off the `-p` stdout shape + permission stack — note two `// remove before commit` debug comments slipped into `provider-manager.ts` upstream, inert for us), #1125 `upgrade`→`update` alias, #1129/#1156 compaction cap, #1128 server `--allowed-host`. v1.4.1 field-name recheck PASS (`command`/`path` intact); 0.20.0 `runShellCommand`/`cancelShellCommand` RPC WATCH discharged (still RPC/TUI-only). **Smoke GREEN** — `bun run smoke:real` 9 pass / 0 fail on the operator's 0.20.2 binary (2026-06-29): review/challenge/ask/review_gate forced writes hook-denied; pursue multi-turn goal wrote zero files (`goal.summary` parsed cleanly, turnsUsed:2 tokensUsed:85770); a read-only swarm subagent's forced write hook-denied; **both write-swarm assertions held** (coder edits confined to the throwaway worktree, patchBytes=334, `userTreeClean=true`; an out-of-trusted-root absolute write hook-denied). The earlier report-88 0.20.0 quota block (provider 403 "usage limit") was the operator-billing-state false alarm, now cleared — the smoke carry-over is **closed** and `{0,20}` is end-to-end proven on 0.20.2. Edits applied: AGENTS.md (hook-engine load-bearing bullet), `docs/upstream-compat-audit.md` (hook-aggregation surface row), `runtime/kimi-version-probe.ts` (`{0,20}` comment extended; array unchanged), this log entry. Consolidates reports 89 (0.20.1) + 90 (forward-scan) + 91 (0.20.2). Tag: `compat-verified-kimi-code-0.20.2`.

- **2026-07-01 (patch release → v1.6.2)** — Daily kimi-code checkup escalated to a certification: npm `latest` and the operator's live binary reached **0.21.1** (0.21.0 + 0.21.1 published the same day), crossing the already-verified `{0,20}` max and firing the H9 "newer than tested max" warning. Verdict **COMPAT-PRESERVED**; extended `KIMI_TESTED_MINORS` with `{0,21}` (the sole runtime change → patch release). Source audit report 93 found the safety surfaces 0-byte across 0.20.2→0.21.1: `02-permission.diff` (policy queue: `PreToolCallHookPermissionPolicy` index 0, `AutoModeApprovePermissionPolicy` index 5, every policy between a DENY) and `03-hooks.diff` (hook engine + any-block-wins aggregation). Also unchanged on load-bearing axes: `options.ts` (argv), `agent/records/` (stream-json record shape), `config/workspace-local.ts` (additional_dir auto-load), and `tools/builtin/file/{write,edit}.ts` (field still `path`). The scoped `run-prompt.ts` change is #1233 shutdown force-exit, which waits only during cleanup after `session.resume_hint` / `goal.summary` have flushed and helps process reaping. New 0.21.0 surfaces are compat-benign for a `kimi -p` wrapper: **plugin slash commands (#1204)** are RPC/host-initiated, absent from `cli/`, not model tools, macro-expand into prompt text that still goes through the index-0 hook, and are main-agent-only; the thinking-effort/model-catalog refactor is provider config plumbing the plugin does not set or consume; compaction rework and record migrations are replay/history internals, with live stdout shape unchanged. The 0.20.0 `runShellCommand`/`cancelShellCommand` RPC WATCH was discharged again (still RPC/TUI-only). **Smoke GREEN** — `bun run smoke:real` 9 pass / 0 fail on the operator's 0.21.1 binary (2026-07-01): review/challenge/ask/review_gate forced writes hook-denied; pursue goal mode wrote zero files (`goal.summary` parsed cleanly, turnsUsed:2 tokensUsed:57595); a read-only swarm subagent's forced write hook-denied; and BOTH write-swarm assertions held (coder edits confined to the throwaway worktree, patchBytes=334, `userTreeClean=true`, `worktreeCleaned=true`; an out-of-trusted-root absolute write hook-denied). New audit checklist item: re-confirm plugin-command activation stays RPC/host-initiated and off the `kimi -p` path. `bun run check` green; version bump 1.6.1 → 1.6.2; tags `v1.6.2` and `compat-verified-kimi-code-0.21.1`. Surface reports: `.claude/kimi-code-research/reports/93-upstream-021-surface.md` and `93-upstream-021-adversarial.md`.

- **2026-07-05 (patch release → v1.6.4)** — Follow-up cert for kimi-code **0.22.3** after report 94's source-only 0.22.0 scan and report 95's incremental 0.22.0→0.22.3 scan. Verdict **COMPAT-PRESERVED**; extended `KIMI_TESTED_MINORS` with `{0,22}` (runtime probe behavior change → patch release). Load-bearing invariants held through 0.22.3: hook engine `03-hooks.diff` 0-byte, policy queue still hook-first (`PreToolCallHookPermissionPolicy` index 0, `AutoModeApprovePermissionPolicy` index 5), permission diff wording-only, `kimi -p` still forces `permission:'auto'` and installs headless handlers, `Write`/`Edit` still use `path`, `Bash` still uses `command`, and `AgentSwarm` still uses the standard stack. New 0.22.x surfaces are compat-benign: prompt-mode background drain happens after assistant-output flush and is still bounded by plugin budgets; shell output caps affect huge tool-result content only; image originals are Kimi session artifacts under KIMI home, not user-worktree writes; model/thinking/provider config changes do not touch permissions; server auth-bypass flags remain a separate subcommand stack. **Smoke GREEN** — `bun run smoke:real` 9 pass / 0 fail against a temp-installed 0.22.3 binary (2026-07-05): review/challenge/ask/review_gate forced writes hook-denied; pursue goal mode wrote zero files (`goal.summary` parsed cleanly, turnsUsed:2 tokensUsed:93029); a read-only swarm subagent's forced write hook-denied; and BOTH write-swarm assertions held (coder edits confined to the throwaway worktree, patchBytes=334, `userTreeClean=true`, `worktreeCleaned=true`; an out-of-trusted-root absolute write hook-denied). Also tightened swarm missing-hook UX: the refusal remains fail-closed but now tells users to repair via Claude Code `/kimi:setup` or Codex `$kimi-setup`; Codex swarm skills mirror that repair guidance without suggesting the skip env. Tags `v1.6.4` and `compat-verified-kimi-code-0.22.3`. Surface reports: `.claude/kimi-code-research/reports/94-upstream-0220-surface.md` and `95-upstream-0223-surface.md`.

- **2026-07-07 (patch release → v1.6.5)** — New-minor cert for kimi-code **0.23.1** after npm `latest` advanced from the certified 0.22.3 line. Verdict **COMPAT-PRESERVED**; extended `KIMI_TESTED_MINORS` with `{0,23}` (runtime probe behavior change → patch release). Load-bearing invariants held through 0.23.1: hook engine `03-hooks.diff` 0-byte, policy queue still hook-first (`PreToolCallHookPermissionPolicy` index 0, `AutoModeApprovePermissionPolicy` index 5), `kimi -p` still uses the PromptJsonWriter-compatible stream-json path, `Write`/`Edit` still use `path`, plugin hooks are still merged any-block-wins, and `AgentSwarm` read/write behavior remained hook-gated in the real smoke. New 0.23.x surfaces are compat-benign: `select_tools` is an upstream default-approved MCP schema-disclosure tool, but the plugin hook still denies unknown tools before default approval and the smoke passed without adding it to `READ_ONLY_TOOLS`; new observability records are persisted agent-log records, not stdout NDJSON shapes; session workDir/index repair, thinking-keep defaults, and workspace-skill listing do not touch plugin permissions. **Smoke GREEN** — `bun run smoke:real` 9 pass / 0 fail against a temp-installed 0.23.1 binary (2026-07-07): review/challenge/ask/review_gate forced writes hook-denied; pursue goal mode wrote zero files and emitted `goal.summary`; a read-only swarm subagent's forced write hook-denied; write-swarm confined coder edits to the throwaway worktree (`patchBytes=334`, `userTreeClean=true`, `worktreeCleaned=true`); and an out-of-trusted-root absolute write was hook-denied. No safety-surface change: no hook allowlist broadening, no swarm bound/default change, and no auto-setup path. Tags `v1.6.5` and `compat-verified-kimi-code-0.23.1`. Surface report: `.claude/kimi-code-research/reports/96-upstream-0231-surface.md`.

- **2026-07-08 (daily checkup, no plugin release)** — kimi-code **0.23.2** landed one day after the 0.23.1 certification, staying inside the already-tested `{0,23}` minor. Verdict **COMPAT-PRESERVED** on the actual released `0.23.2` tag (`67b2147d`). **No version bump / no `KIMI_TESTED_MINORS` change** — this is a patch checkup inside an already-certified minor, so the right outcome is a docs/comment update plus a compat marker tag, not a new plugin release. Findings:
  - **Permission and session-bootstrap surfaces stayed untouched.** `02-permission.diff` was 0 bytes (policy queue unchanged: `PreToolCallHookPermissionPolicy` still index 0, `AutoModeApprovePermissionPolicy` still index 5), and `05-session-bootstrap.diff` was 0 bytes (no new `additionalDirs` / config-load drift on the `-p` path).
  - **The scoped `run-prompt.ts` delta is operational, not semantic for our parser.** #1483 changes `raceWithTimeout()` so the cleanup timer stays ref'd; a failed headless `kimi -p` turn can no longer drain the event loop and exit 0 before the rejection propagates. This strengthens exit-code correctness for failure cases but does not alter `permission:'auto'`, `installHeadlessHandlers`, `PromptJsonWriter`, `session.resume_hint`, or `goal.summary`.
  - **The non-empty hooks diff is a Windows UX spawn fix, not a hook-contract change.** #1466 extracts `buildHookSpawnOptions()` and adds `windowsHide:true` when spawning hook child processes. Stdin JSON shape, exit-2-as-deny semantics, cwd/env forwarding, and any-block-wins aggregation all remain intact. The non-empty `04-wire-records.diff` was only this same `session/hooks/runner.ts` hunk re-included by the broader `session/` scope — there was no NDJSON output-shape drift.
  - **Smoke GREEN** — a temp-binary `bun run smoke:real` against exact `0.23.2` ran **9 pass / 0 fail** (2026-07-08): review/challenge/ask/review_gate forced writes hook-denied; pursue goal mode wrote zero files and parsed `goal.summary`; a read-only swarm subagent's forced write hook-denied; write-swarm confined coder edits to the throwaway worktree (`patchBytes=306`, `userTreeClean=true`, `worktreeCleaned=true`); and an out-of-trusted-root absolute write was hook-denied.
  - Edits applied: `runtime/kimi-version-probe.ts` (0.23.2 patch-checkup comment after `{0,23}`; no array change), `runtime/stream-json.ts` (verified-through widened to 0.23.2), AGENTS.md (Version/compat note), this log entry, rebuilt `dist/`. Tag: `compat-verified-kimi-code-0.23.2`. Continuity report: `.claude/kimi-code-research/daily-monitor/2026-07-08-upstream-monitor.md`.

- **2026-07-09 (daily checkup, no plugin release)** — kimi-code **0.23.3** and **0.23.4** landed after the 0.23.2 marker, staying inside the already-tested `{0,23}` minor. Verdict **COMPAT-PRESERVED** on the actual released `0.23.4` tag (`7ce3c2dc`). **No version bump / no `KIMI_TESTED_MINORS` change** — one latest-patch compat marker covers both patches. Findings:
  - **The safety chain is byte-identical.** `02-permission.diff` and `03-hooks.diff` were both 0 bytes across 0.23.2→0.23.4. `PreToolCallHookPermissionPolicy` remains index 0, `AutoModeApprovePermissionPolicy` remains the first approve at index 5, every policy between them is a deny, hook aggregation remains any-block-wins, and `Bash.command` / `Write.path` / `Edit.path` are unchanged. AgentSwarm still uses the standard permission stack; the `DenyAllPermissionPolicy` insertion remains `startBtw()`-only.
  - **The `run-prompt.ts` change repairs the path consumed by `/kimi:pursue`.** #1516 makes `kimi -p "/goal ..."` wait across continuation turns until the goal becomes terminal instead of resolving after the first completed turn. `permission:'auto'`, `installHeadlessHandlers`, PromptJsonWriter, `session.resume_hint`, and `goal.summary` shapes remain intact; `PromptTranscriptWriter.flushAssistant()` only fixes text-mode finalization. This is a load-bearing lifecycle change, so the real-binary smoke was required.
  - **The non-empty wire/bootstrap diffs are compat-benign internal plumbing.** 0.23.3's #1506 changes a repeated provider 401 from a misleading `AUTH_LOGIN_REQUIRED` to `PROVIDER_AUTH_ERROR` carrying the upstream reason. 0.23.4 adds internal `media-degraded` request-projection observability and owner-scoped `[image]` limits threaded through create/resume sessions. None adds a PromptJsonWriter record or changes `additionalDirs`, plugin-hook merging, or permission construction.
  - **Smoke GREEN** — the exact body of `bun run smoke:real`, pointed via `KIMI_PLUGIN_CC_KIMI_BIN` at a temp-installed **0.23.4** binary, ran **9 pass / 0 fail** (2026-07-09): review/challenge/ask/review_gate forced writes hook-denied; pursue ran across the full two-minute budget (`aborted=true`, `exit=143`) with repeated denials and zero files; a read-only swarm subagent's forced write was hook-denied; write-swarm confined coder edits to the throwaway worktree (`patchBytes=278`, `userTreeClean=true`, `worktreeCleaned=true`); and an out-of-trusted-root absolute write was hook-denied.
  - Edits applied: `runtime/kimi-version-probe.ts` (0.23.3/0.23.4 patch-checkup comment alongside `{0,23}`; no array change), `runtime/stream-json.ts` (verified-through widened to 0.23.4), AGENTS.md (compat range), this log entry, rebuilt `dist/`. Tag: `compat-verified-kimi-code-0.23.4`. Continuity report: `.claude/kimi-code-research/daily-monitor/2026-07-09-upstream-monitor.md`.

- **2026-07-11 (safety stabilization release -> v1.8.0 + patch checkup)** — kimi-code **0.23.5** stayed inside the already-tested `{0,23}` minor, but added one legitimate `kimi -p --output-format stream-json` record that required a parser update. Verdict **COMPAT-PRESERVED after the v1.8.0 parser change** on released tag commit `352a4492`; no `KIMI_TESTED_MINORS` array change. Findings:
  - **The safety chain is byte-identical.** `02-permission.diff`, `03-hooks.diff`, and `05-session-bootstrap.diff` are all 0 bytes across 0.23.4->0.23.5. Hook-first ordering, any-block-wins aggregation, `Bash.command` / `Write.path` / `Edit.path`, and create/resume config loading remain intact. The non-empty wire diff only adds the internal `media-stripped` projection type.
  - **Retry metadata is now modeled rather than misdiagnosed.** Upstream #1530 emits `role:"meta", type:"turn.step.retrying"` with retry counters, delay, error identity, and optional status code. Before the fix, the parser correctly rejected it as an unknown meta type; v1.8.0 validates the exact upstream shape, normalizes it, and filters it from consumer `records[]` / `onRecord` like `session.resume_hint`. Malformed variants still enter parser diagnostics.
  - **Safety stabilization shipped in the same release.** Every model-spawning command now verifies the hook before Kimi starts; setup parses and validates the complete shared config under an identity-bound serialized lock; and cancellation is an identity-safe bounded teardown barrier before caller settlement. These are plugin-side hardening changes, not upstream permission relaxations.
  - **Smoke GREEN after the parser change.** The exact body of `bun run smoke:real`, pointed at a temp-installed exact **0.23.5** binary, ran **9 pass / 0 fail in 229.56s**: review/challenge/ask/review_gate forced writes were denied; pursue ran through its full two-minute budget with no file written; read-only swarm spawned coder subagents whose writes were denied; write-swarm confined edits to the throwaway worktree (`patchBytes=334`, `userTreeClean=true`, `worktreeCleaned=true`); and an out-of-trusted-root absolute write was denied. The first post-edit full run had one inconclusive read-swarm case because the coordinator declined the requested fan-out and wrote nothing; the smoke prompt was made explicit about its isolated authorized safety-test role, a targeted proof passed, and this final full rerun passed all nine assertions.
  - Edits applied: parser/runtime coverage, v1.8.0 safety stabilization, `runtime/kimi-version-probe.ts`, `runtime/stream-json.ts`, AGENTS/CHANGELOG/safety docs, and regenerated release surfaces. Tags: `v1.8.0` and `compat-verified-kimi-code-0.23.5`. Continuity report: `.claude/kimi-code-research/daily-monitor/2026-07-11-upstream-monitor.md`.

- **2026-07-12 (daily checkup, no plugin release)** — kimi-code **0.23.6** stayed inside the already-tested `{0,23}` minor. Verdict **COMPAT-PRESERVED** on released tag commit `b5c236d0`. **No version bump / no `KIMI_TESTED_MINORS` change** — this is a docs/comment update plus compat marker, not a plugin release. Findings:
  - **The deny chain is byte-identical.** `02-permission.diff` and `03-hooks.diff` were both 0 bytes across 0.23.5→0.23.6. `PreToolCallHookPermissionPolicy` remains index 0, `AutoModeApprovePermissionPolicy` remains the first approve at index 5, every intervening policy is a deny, hook aggregation remains any-block-wins, and `Bash.command` / `Write.path` / `Edit.path` are unchanged.
  - **Prompt-mode lifecycle changes stay inside plugin bounds.** #1555 keeps `kimi -p` alive while a goal is active or cron work has a future fire; #1497 adds opt-in `[background].print_background_mode = "steer"` for follow-up turns after background completions. The plugin does not enable steer mode, and pursue/swarm retain mandatory finite AbortController budgets, so neither change removes the owning process ceiling. No PromptJsonWriter record shape changed.
  - **Longer upstream subagent defaults do not widen plugin cost or write bounds.** #1562 makes subagent timeout configurable and raises the upstream default from 30 minutes to two hours. AgentSwarm still uses the standard permission stack; the plugin's tighter wall-clock budgets and hard concurrency env remain independent outer bounds. The `select_tools`→`dynamically_loaded_tools` provider-capability rename is internal disclosure plumbing; read and write AgentSwarm both launched in field smoke without permission or hook changes.
  - **Smoke GREEN** — after two initial model non-dispatches in the adversarial escape lane (no files landed), a targeted exact-0.23.6 run produced a real AgentSwarm dispatch and passed. The subsequent complete exact-binary suite ran **9 pass / 0 fail in 274.55s**: review/challenge/ask/review_gate forced writes were denied; pursue ran to its full two-minute budget with no file; read-only swarm denied a spawned subagent write; write-swarm stayed confined (`patchBytes=306`, `userTreeClean=true`, `worktreeCleaned=true`); and the separate out-of-root absolute write was hook-denied.
  - Edits applied: `runtime/kimi-version-probe.ts` and `runtime/stream-json.ts` compatibility comments, AGENTS.md compat range, this log entry, and regenerated runtime mirrors. Tag: `compat-verified-kimi-code-0.23.6`. Continuity report: `.claude/kimi-code-research/daily-monitor/2026-07-12-upstream-monitor.md`.

- **2026-07-15 (new-minor certification → v1.8.1)** — kimi-code **0.24.2** crossed the tested `{0,23}` max after 0.24.0/0.24.1 shipped on July 14, firing the H9 newer-than-tested warning. Verdict **COMPAT-PRESERVED**; extended `KIMI_TESTED_MINORS` with `{0,24}` (runtime probe change → patch release). Findings:
  - **Four-lane source audit green.** Across 0.23.6→0.24.2, `02-permission.diff` and `03-hooks.diff` are 0 bytes. PreToolUse remains index 0, AutoModeApprove remains the first approve at index 5, every intervening policy denies, any-block-wins aggregation is unchanged, and `Bash.command` / `Write.path` / `Edit.path` remain stable. Reports 97-100 independently covered hook contract, stream-json, CLI/bootstrap, and an adversarial challenge; synthesis is report 101.
  - **Default v1 prompt mode remains compatible.** The refactor extracted rendering/session helpers but preserved explicit `-p`/`-r`/`--output-format stream-json`/`-m`/`--skills-dir`, forced auto permission, assistant/tool/retry/goal/resume shapes, and terminal session pinning. New default background steering and upstream no-timeout task waits remain inside the plugin's finite budgets, hard swarm concurrency caps, and identity-safe cancellation settlement barrier.
  - **Ambient experimental v2 was not waved away.** A truthy `KIMI_CODE_EXPERIMENTAL_FLAG` selects native agent-core-v2. Source tracing found strict hook config, configured+plugin hook merging, the same snake_case payload and exit-2 deny semantics, awaited pre-execution blocking, and a per-agent hook service. A targeted exact-v2 review smoke denied the forced write (1 pass / 8 skip / 0 fail). V2 prepends `role:"meta", type:"system.version"`; the current parser safely logs it as one non-fatal diagnostic, excludes it from consumer records/prose, and still pins `session.resume_hint`, so explicit modeling is optional rather than certification-blocking.
  - **Exact-binary v1 smoke GREEN.** The temp-installed exact 0.24.2 suite ran 9 pass / 0 fail, 39 assertions in 261.33s: review/challenge/ask/review_gate forced writes denied; pursue ran through its full two-minute budget with zero writes; read swarm denied a spawned subagent write; write-swarm stayed worktree-confined (`patchBytes=278`, `userTreeClean=true`, `worktreeCleaned=true`); and the separate out-of-root absolute write was denied.
  - **Documentation correction.** `packages/agent-core/src/session/hooks/` is the live v1 engine; the historical `agent/hooks/` tree was removed by relocation commit `74e867a`, so audit scripts retain both paths only to cover older layouts. Edits: tested-minor extension, v1.8.1 metadata/docs, stream source comment, audit-playbook correction, tests, and regenerated surfaces. Tags: `v1.8.1` and `compat-verified-kimi-code-0.24.2`. Continuity report: `.claude/kimi-code-research/daily-monitor/2026-07-15-upstream-monitor.md`.
