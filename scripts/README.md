# scripts

Placeholder directory for runtime-facing entrypoints.

Planned files:

- `kimi-companion.mjs`
- additional runtime command wrappers as needed

The companion entrypoint is the stable boundary between Claude command markdown and the local runtime.
